# Phase 1 validation — 21 September 2026

## Passing checks

- Fresh remote fetch completed; redesign branch was created from `origin/staging` at `2707efa`. Staging/master were identical at that point. Branch upstream was removed to avoid accidental pushes to staging.
- Frontend dependency install: `npm ci --legacy-peer-deps --no-audit --no-fund` completed. The installer rewrote yarn.lock incidentally; that generated-only change was restored. Both original lockfiles are unchanged.
- Studio dependency install: `yarn install --frozen-lockfile --ignore-scripts` completed using the existing Yarn lockfile. No package/lock updates were made. See `validation/studio-install.txt` for peer warnings. Studio build verified the resulting installation.
- Frontend: `next build` completed successfully. This intentionally skipped the buildJson pre-step to preserve the baseline tracked settings JSON. See `validation/frontend-build-baseline.txt`. Existing Next config suppresses type/lint failures; this is a compilation/render check, not a clean type/lint result.
- Studio: `SANITY_STUDIO_TELEMETRY_DISABLED=1 yarn build` completed successfully. See `validation/studio-build-baseline.txt`.
- Local development `/work`: HTTP 200 with `X-Robots-Tag: noindex, nofollow, noarchive`.
- Preview classifier: seven environment cases passed (explicit redesign, Vercel preview, staging, development enabled; empty/default, production and explicit zero without another preview signal disabled).
- Manifest: Otherness naming and both icon paths resolve to the staged public assets.
- `git diff --check` passed.
- Legacy page/component/global-style/schema/query/types/settings source files were unchanged. `baselines/legacy-source-sha256.json` records source hashes for comparison in subsequent phases.

## Existing failures, not introduced by the redesign

- Frontend `tsc --noEmit --incremental false`: **13 errors** across legacy files. Exact baseline in `validation/frontend-typecheck-baseline.txt`. Includes media metadata types, Mux props, ES5 iteration, project collaborator type and empty context module. Re-running after phase 1 produced identical output (no added errors).
- Frontend ESLint does not start: `.eslintrc` contains `"arrow-body-style": "always"` rather than a severity/options tuple. Exact output in `validation/frontend-eslint-baseline.txt`. Do not call the lint baseline green.
- Studio `npm ci` fails because package-lock.json does not match package.json (missing existing Sanity plugin entries). Use existing Yarn lockfile; do not silently upgrade dependencies to repair npm's alternate lockfile.
- Studio `tsc --noEmit --incremental false`: **5 errors**, including implicit Rule type, orderable-document-list/Sanity type mismatch and JSX namespace. See `validation/studio-typecheck-baseline.txt`.
- Project pages Medable and Artem show development hydration errors. Medable browser output is saved in `validation/medable-browser-errors.json`; the rendered tree contains nested paragraphs, among other reported mismatches. Error overlay was dismissed for visual baseline capture; the bug was not fixed.
- Legacy layout reads stale checked-in settings: old booking URL and email differ from current published Sanity values. This was intentionally retained in the legacy baseline. New settings must be seeded from fresh CMS data in phase 2.

## Browser baseline

Only Codex in-app browser was used, with viewport overrides reset afterward.

| Route | Capture | Purpose |
| --- | --- | --- |
| `/work` | `baselines/work-desktop.png`, `work-mobile.png` | Listing typography, header, filter controls, first cards |
| `/work/medable` | `baselines/medable-desktop.png`, `medable-mobile.png` | Full-width hero project, intro/services/subprojects |
| `/work/artem` | `baselines/artem-desktop.png`, `artem-mobile.png` | Two-column hero project, intro/services |

Desktop: 1512 × 982. Mobile: 402 × 874. `/work` desktop DOM viewport/document widths were both 1512 (no page overflow observed). Captures are viewport baselines, not full-page approval screenshots. Media frames are dynamic; compare layout/type rather than exact video pixels. Browser panel visibility was disabled for final desktop captures to avoid native-panel clipping.

`baselines/project-inventory.json` records published project hero types and archive flags. Filter interaction, all subproject navigation, video controls, archive redirects and the complete responsive matrix still need dedicated regression tests during shared-chrome/final QA phases. Baseline capture is not a claim that those scenarios all passed.

## Hosting boundary

Local preview isolation/noindex is verified. No hosted preview has been created; no Git push, Vercel link, remote deployment or Sanity hook inspection/mutation has occurred. No Vercel/Sanity connector was available in this session. Phase 2 must inspect actual hosting access/configuration before establishing its dedicated rebuild target. Do not describe deployed hooks as verified based on the local files alone.

## Staged files and next session

New tokens are opt-in exports in `frontend/styles/redesign.ts`, not imported into legacy global styles. Brand files are under `/redesign/` and are not wired into document metadata until phase 3. All 22 design screenshots are in `references/`.

Neue Montreal, new clean artwork and Contact designs remain pending. See ASSETS.md.

# Phase 2 validation — 21 September 2026

- `node studio/scripts/verify-redesign.cjs`: PASS; seven schema types compile, groups resolve, image alt/hotspot configured, Contact excluded.
- `npm run verify:redesign`: PASS; fixed IDs, fresh settings/normalized email, service order, resolved project cards and stable keys, real Noticed links, null assets/documents and unresolved-reference diagnostic; all 182 legacy source hashes match.
- Seed dry-run read fresh published settings revision `9sbb9mCKcoDYNnVJUsvUcN`. First `--apply --cli-auth` created 3 documents; second created 0. All 642 pre-existing non-draft documents retained identical IDs/revisions after both runs.
- Studio build and deployment: PASS; additive schema/config deployed to https://otherness.sanity.studio/.
- `npm run build:redesign`: PASS; 33 routes generated; legacy buildJson skipped; fresh redesign snapshot isolated under ignored `.redesign/`.
- Frontend full typecheck: same 13 inherited errors; no redesign errors. Studio full typecheck: same 5 inherited errors (Rule and orderable-list type mismatches). Phase 1 prose mentioned JSX namespace, but the saved baseline log contains only these five errors. No added errors.
- Targeted ESLint on new frontend modules/scripts and Studio schema/verification script: PASS using `--no-eslintrc --config frontend/.eslintrc.redesign.cjs`. The legacy ESLint configuration remains invalid.
- Release content check: intentionally FAIL, exit 1, 76 actionable missing copy/artwork/alt fields. Preview builds remain usable. This is not visual acceptance; supplied artwork/font dependencies remain.
- Installed Studio CLI does not support `sanity schema extract`; explicit nullable frontend types plus query/schema checks are used without upgrading dependencies.
- Local Studio origin 3334 was rejected by existing CORS; switched to the already allowed 3333. Browser then reached Sanity sign-in. No CORS or access policy changes were made; authenticated UI editing remains unverified.
- Exact build/type/data outputs are under `validation/phase2-*`. Hosting completion is recorded below after deployment verification.

## Phase 2 hosted setup and clean-install findings

Actual authenticated Vercel and Sanity APIs were inspected before changes. Existing Sanity Vercel tool entries were Staging and Production; automatic Sanity hooks were empty. The Vercel project uses root `frontend`, Node 24, `master` for production, with existing hooks for `master` and `staging`.

Added one Website Redesign deploy hook for `codex/site-redesign`, plus a private Studio Vercel-tool entry reusing its existing credential. Manual hook POST returned HTTP 201 and created a build for the correct branch/commit. No automatic content publication hook was installed, matching the existing manual rebuild workflow. Existing hooks and all 642 pre-existing CMS revisions remained unchanged after setup.

First hosted build failed because `@react-spring/three` was only a peer dependency; second reached a missing `react-is` peer. Both were already present locally and locked at 9.7.3 / 18.2.0. Added exact direct dependency declarations and minimal matching lockfile changes. No dependency versions or frameworks were upgraded. Failures are saved as `phase2-hosted-build-first.txt` and `phase2-hosted-build-second.txt`.

Local `/work` was inspected in the in-app browser: legacy navigation, filter controls, project list and baseline footer remain. HTTP 200 with `X-Robots-Tag: noindex, nofollow, noarchive`. This is a smoke check; full Work interaction regression remains phase 3/final QA.

Staging and public aliases were inspected through Vercel: they still resolve to their original deployment IDs, `dpl_EzsX8AUMtSntpbBQNUrH9Q9WDg6N` and `dpl_2hW8KVM2WdqKzcff48GcNwxsi41B` respectively. No alias was assigned or repointed by this task.

## Phase 2 final hosted result

PASS: commit `e7f70af5351d14f82e3350290846db8b898f0e80` built successfully from a clean Vercel install. A subsequent **Website Redesign hook rebuild** completed READY as `dpl_8oWbJh7GSjY6pgf4avQKSR2GYm96`, proving the dedicated CMS rebuild destination works. Build logs show all 3 published redesign documents fetched and 33 legacy routes generated. No content writes were needed to test the hook.

The stable preview `https://otherness-git-codex-site-redesign-tayteco-36dd2d0b.vercel.app/work` was inspected in the in-app browser. `/work`, `/robots.txt` and `/sitemap.xml` all return HTTP 200 with `noindex, nofollow, noarchive`; robots disallows `/`, and sitemap entries use the preview origin rather than staging. See `phase2-hosting.json` and `phase2-hosted-build-success.txt`.

Phase 2 is complete. Website Redesign content is editable in the deployed Studio, though authenticated browser editing remains untested because that browser session is logged out. Phase 3 is next; no chrome/page rendering was changed in this phase.

Final workspace note: 18 independently supplied Neue Montreal OTF/TTF files appeared after the implementation push. `file` validated their font containers. They remain untracked and untouched for phase 3; this task did not add them to its commits or deploy them. Asset register and handoff updated accordingly.

# Phase 3 validation — 21 September 2026

## Implementation and automated checks

- Shared header/footer and a native modal mobile menu now read `siteSettingsV2` through additive `getRedesignShellProps()` in each page's `getStaticProps`, including 404. Legacy page data contracts remain intact. No CMS content or schema writes were made.
- `npm run build:redesign`: PASS, 33 static-generation entries. Preview build still bypasses legacy buildJson. `SANITY_STUDIO_TELEMETRY_DISABLED=1 yarn build`: PASS.
- `npm run verify:redesign`: PASS, 181 preserved legacy hashes: 179 byte-identical files plus both Work pages matching after removal of the two additive shell-plumbing lines. Only shared Layout is excluded from the original 182-file baseline. Baseline hashes were not rewritten.
- `npm run verify:redesign:chrome`: PASS, 30 concrete built routes have published settings, and 18 project/article OG overrides are preserved. Every HTML file (including the built-in 500 and dynamic fallback templates) has exactly one absolute OG image and the new manifest. Emergency/fallback templates have no page props by Next design; their default image uses the build origin.
- Metadata verification caught an inherited article path mismatch (`openGraphImage.image.asset` versus the actual `openGraphImage.asset` contract). Fixed only the metadata access. Full frontend typecheck now has **11 inherited errors**, reduced from 13 by those two metadata corrections; no new errors. Studio retains the same **5 errors**.
- Targeted ESLint PASS for new chrome, shell loader, Layout, scoped styles and verification scripts, including React Hooks rules and image/anchor accessibility rules. Global legacy ESLint remains invalid; it was not repaired or called green.
- Release content check intentionally FAILS with the same 76 missing content/artwork/alt fields. Fonts are ready, but this is not final visual/content acceptance.
- HTTP checks PASS for Work, Privacy, Working Together, icons/manifest, OG asset, font, robots and sitemap. All carry `noindex, nofollow, noarchive`. `/our-way` returns the expected temporary 404 until phase 6; no redirect was added to Working Together.

Exact logs and captures: `validation/phase3-*`.

## Browser checks (Codex in-app browser only)

- Desktop 1512×982 and mobile 402×874 captures of Work, Medable and Artem; menu and desktop/mobile footer captures saved. Header is intentionally top-cropped; compact navigation remains fixed after scrolling. Full-width mobile Menu bar, 44px controls, active Work state and desktop-only footer wordmark strip verified.
- Responsive checks at **375, 402, 768, 1024, 1512 and 1920**: document width equals viewport width in every case. Tablet wordmark is capped to preserve Work's existing content clearance. Footer heading stays on two lines; social/legal rows wrap without overflow.
- Menu: open focuses Close; Shift+Tab wraps to Email; Tab wraps back to Close; Escape and Close return focus to Menu. Native modal makes the background inert. Scrolling while open does not move the underlying page. Close restores **1748px → 1748px** exactly. A discovered Lenis cached-height issue was fixed by resizing Lenis after releasing the body lock.
- Menu closes on real Medable → Work navigation, same-route Work selection, Our Way → 404 navigation, and mobile → desktop resize. Body styles are restored; native scrolling resumes. New chrome transitions have explicit reduced-motion CSS; OS reduced-motion emulation was not available through the in-app capability, so that branch was source-checked rather than interactively emulated.
- Work retains Classic Grotesque/Baryton; chrome resolves to Neue Montreal. Listing body alignment and card widths match the phase 1 reference. **Baseline limitation found:** the phase 1 Medable/Artem desktop PNGs captured an effectively blank transition state, so they cannot prove a pixel-level project-body match. The preserved source checks plus new rendered desktop/mobile captures verify those layouts; comprehensive visual regression remains phase 8.
- Existing Medable/Artem development hydration failures (including nested paragraphs) remain. Development captures retain their error indicator; production-build browser checks also render both project layouts. No new chrome hydration error was observed.
- Work filters initially failed because both preview origins were absent from Sanity CORS. Added exactly `http://localhost:3010` and the stable redesign preview origin with **credentials disabled**, preserving all seven existing origins. Both now return HTTP 200 with their exact allowed origin and no allow-credentials header. No wildcard or authenticated browser access was added. See `phase3-cors.json`.
- After CORS correction: Artsy returns Famille Elastique, Harper, Confidential and Saima Zaidi; All restores 16 visible project links; Digital returns Harper and The Gorgeous Spice Company. `/work/black-and-free` retains its client redirect to `/work`. Privacy navigation passes. Contact/social hrefs match published settings; no booking, email or social message was sent.

## Remaining boundaries

Our Way is a phase 6 routing dependency; Contact remains the current booking destination until its phase 7 design. The legacy homepage content now has one shared header/menu; its final landing position and scroll choreography belong to phase 5. Trademark is absent until approved CMS copy exists. Full video-control, every subproject, load-more edge cases and full accessibility/performance acceptance remain part of phase 8; do not infer those from the representative checks above.

## Hosted phase 3 result

PASS: implementation commit `6c7605c7568f45d752c57aaa61021b4594bcaf00` built READY as `dpl_DNhdLzJjwpgkmZTt8g6nf7ouiqZE`. The stable preview serves the new chrome; in-app browser verified its mobile menu/Escape/focus return and absolute preview-origin OG image. Hosted Work, Medable, article, font, manifest, OG, robots and sitemap requests all pass with noindex. Project/article image overrides remain single tags. See `phase3-hosting.json` and `phase3-hosted-http.json`.

Staging still resolves to `dpl_EzsX8AUMtSntpbBQNUrH9Q9WDg6N`; public www still resolves to `dpl_2hW8KVM2WdqKzcff48GcNwxsi41B`. No aliases, hooks, production branches or CMS documents were changed by deployment. Git pushes use the explicit redesign branch only.

Additional representative check: Medable's mobile Product navigation scrolls to its section heading at 100px below the viewport top. Full controls/all sections remain phase 8. Final Work screenshots were recaptured after the existing page animation settled (opacity 1); desktop header footprint is 202px, matching baseline body positioning.

Unattended commit creation used a one-command signing override because 1Password signing required interaction. Persistent Git signing configuration was not changed.

# Phase 4 validation — 21 September 2026

## Implementation and checks

- `npm run build:redesign`: PASS, 33 static-generation entries; all 3 published redesign documents fetched. Home now renders the four new sections. Temporary fixture route is absent from the production build. Legacy HomeHero remains for phase 5.
- `SANITY_STUDIO_TELEMETRY_DISABLED=1 yarn build`: PASS. `node studio/scripts/verify-redesign.cjs`: PASS; seven types and fixed-ID/null/unresolved-reference query fixtures. No Studio source or CMS writes.
- `npm run verify:redesign`: PASS; same 181 preserved source hashes, with Work shell props normalized. All legacy Home blocks remain untouched on disk.
- `npm run verify:redesign:chrome`: PASS; 30 built routes have published shell props, 18 page image overrides remain, one absolute default image/manifest per HTML.
- Targeted ESLint with `.eslintrc.redesign.cjs`, React Hooks rules, image alt and anchor-content rules: PASS for HomeSections, scoped styles, Artwork and homepage. Global inherited ESLint configuration remains invalid.
- Frontend `tsc --noEmit --incremental false`: same **11 inherited errors**, no new errors. Studio: same **5 inherited errors**. Exact logs saved under `validation/phase4-*`.
- `npm run check:redesign:release`: expected FAIL, exit 1, same **76 missing content/artwork/alt fields**. Preview is functional, not final content/artwork acceptance.
- `git diff --check`: PASS. No dependency, font, global-style, Work, shared-chrome, schema, content or hook changes.

## In-app browser checks

Only the Codex in-app browser was used; desktop panel hidden for captures. Local dev and production builds checked. Viewport captures are under `validation/phase4-*`; a defective full-page stitching capture was discarded in favour of viewport captures.

- At **375, 402, 768, 1024, 1512 and 1920**, document width equals viewport width. Baryton headings and Neue Montreal section text are scoped correctly. Desktop shows three varied card image proportions; mobile retains every card with the next card visible. At 402px the Branding track is 384px wide with 869px content; horizontal scrolling moved from 0 to 485px. At 375px it is 357px/808px.
- Branding is initially open. Enter closes it; Space opens Strategy; reopening Branding closes Strategy. Hidden panels are excluded from focus/accessibility. Published order is Branding, Strategy, Art direction, Packaging, Digital at all widths.
- Final capture review found a thumbnail overlapping the next mobile row. Expanded row height now follows the responsive thumbnail: measured lower clearance is 22px at 375 and 17px at 402/768, with no page overflow. See `phase4-noticed-clearance.json`.
- Mobile Noticed begins with the first row expanded. Clicking Flydog closes the first and expands Flydog without navigating; Enter closes Flydog. Destination links are separate. Desktop keyboard focus activates its row and thumbnail. CSS/React hover handlers support pointer entry/exit; explicit mouse-hover emulation was not available in the locator API.
- Production-build service card navigation reaches `/work/aero` and its existing body/subproject controls. This is representative navigation plus source preservation, not a repeat of comprehensive Work media/filter QA from phase 3/8.
- Local fixture verifies results A→B by ArrowRight, Home→A, End→B, and click→A, with matching background, logo, quote and selected attribution. Current published content contains only one result, so no additional live testimonial was invented. Native horizontal scrolling and real buttons support touch; a physical touch-device test was not performed.
- Fixture verifies a null Home/settings document, null services/results/Noticed and unresolved project reference render placeholders without fabricated destinations or crashes. Fixture source is saved as `validation/phase4-fixture.tsx.txt`; it was removed from `pages/` before build.
- Current missing artwork/alt fields render labelled placeholders. Required image failure fallback is implemented; final supplied-artwork crops and the optional mobile-background override need visual acceptance when originals arrive.
- The legacy homepage Mux player reports a style hydration warning. Its source was unchanged; it remains a phase 5 replacement dependency. It also retains its legacy hero booking destination; the new section CTAs use the fresh published consultation URL.

## Boundaries and dependencies

The loader/session handling, replacement landing, header scroll choreography and `/our-way` remain phases 5/6. Introduction statement, service descriptions and curation/captions, new artwork, client logos and Noticed thumbnails are still pending. No supplied screenshot became production artwork. No animation, autoplay or reduced-motion-dependent behavior was added to phase 4 sections.

## Hosted phase 4 result

PASS: final application commit `9a5d4ef8d7eaa74be30a1eaf349b26cb00c3c702` is READY as `dpl_7t8MJmVxcFmNPPDCn5HE2z92FakM`. Stable preview homepage returns HTTP 200 with `X-Robots-Tag: noindex, nofollow, noarchive`. In-app browser confirms five services, one published result and ten Noticed rows; Strategy opens while Branding closes; Flydog expansion remains separate from its real destination. Final deployed expanded-row minimum height is 184px at 402px, with 17px clearance below the thumbnail and document width 402px. The OG image remains one absolute preview-origin URL.

Vercel API confirms staging still resolves to `dpl_EzsX8AUMtSntpbBQNUrH9Q9WDg6N` and public www to `dpl_2hW8KVM2WdqKzcff48GcNwxsi41B`. No aliases/hooks were changed. `phase4-hosting.json` records final identities; `phase4-hosted-http.json` records Home New payload/section/noindex checks (initial phase 4 deployment; final spacing fix confirmed separately in the hosted browser).

Browser viewport override was reset and the stable preview left open at the introduction. Local preview processes were stopped. Untracked supplied font originals remain untouched. Commits used a command-scoped signing override, preserving persistent Git signing settings.

# Phase 5 validation — 21 September 2026

## Implementation and automated checks

- `npm run build:redesign`: PASS, 33 static-generation entries; three published redesign documents fetched. Temporary fixture route removed before build. Home no longer fetches legacy Home or loads HomeHero/Mux; phase 4 sections remain unchanged.
- `SANITY_STUDIO_TELEMETRY_DISABLED=1 yarn build` and `node studio/scripts/verify-redesign.cjs`: PASS. No Studio changes or CMS writes.
- `npm run verify:redesign`: PASS, all 181 preserved legacy source hashes (Work additive shell props normalized). No Work body/font/global-style changes.
- `npm run verify:redesign:chrome`: PASS, 30 concrete routes with shell props, 18 page OG overrides preserved, exactly one absolute OG image/manifest per HTML.
- Targeted ESLint with isolated config, Hooks and image-alt rules: PASS. Global inherited ESLint config remains invalid.
- Explicit frontend typecheck: same **11 inherited errors**; Studio: same **5 inherited errors**. No new errors. `npm run check:redesign:release`: expected FAIL with the same **76 missing fields**. Frontend build suppresses type/lint errors, so these were checked separately.
- `git diff --check`: PASS. No package/dependency, font, schema, CMS, hook or phase 4 section changes. Homepage now uses `initial={false}` so server-rendered content stays visible independently of the intro/JS animation.

## In-app browser checks

- Only Codex in-app browser used; hidden panel for 1512px desktop captures. **375, 402, 768, 1024, 1512 and 1920** all have document width equal to viewport width. Landing height is 874px at the smaller reference viewports and 982px on desktop. At 402px, wordmark is 174px wide at y=766; Menu is 354×44 at x=24, y≈814. Desktop 1512px wordmark is 226px wide at y=850, navigation ends at 953px.
- Intro timing fixture with failed artwork recorded Poetry/Power at 12ms, Strategy/Design at 1515ms, Endurance/Distinction at 3016ms, closure at 4817ms. This includes the three 1.5s pairs and final 0.3s landing fade. Image failure renders a labelled placeholder; intro finishes without waiting for media. See `phase5-intro-timing.json` (recorded before visible button removal).
- First visit opens the intro; refresh and real Home → Work → Home navigation do not replay. Separate tab plays independently. Native modal prevents background interaction; completion restores overflow and focuses the landing statement.
- **User-directed change:** visible Skip intro button was initially tested, then removed at Tayte’s request. Final production build has no button in the intro; Escape still dismisses immediately. SPEC.md was updated to match. Older fixture logs mention Skip intro only as historical evidence, not final UI.
- Reduced-motion, unavailable storage and null Home fixtures bypass the intro without a scroll lock or crash. OS motion emulation is unavailable in the browser capability; the local fixture overrides only the motion media query, exercising the runtime admission branch. Dynamic OS preference switching is source-checked, not claimed as an OS-level interaction test.
- Broken artwork URL was requested and returned 404, then replaced with the labelled placeholder. `phase5-fixture-results.json` records the initial in-flight image state; `phase5-intro-timing.json` records the eventual placeholder and automatic close. Fixture source is retained as `.tsx.txt` only and is absent from routes/build.
- Desktop scroll: at 802px the wordmark is 1134px wide with top −77.77px, intentionally cropped; after 90% of landing height compact navigation stays at y=16px. Responsive resize recalculates positions. Motion preference uses discrete positions instead of continuous header travel.
- Landing mobile Menu opens with focus on Close; Escape returns focus to Menu, restores body position/scroll and closes the native modal. Work route navigation was exercised and all source-preservation hashes pass. Comprehensive Work/media QA remains phase 8.
- Clean artwork is still missing; these captures verify geometry/typography/interactions, not final artwork/crop acceptance. No legacy image or screenshot crop was substituted. The old homepage Mux hydration warning is gone with its removal; inherited project hydration issues remain outside phase 5.

## Boundaries

Our Way is phase 6 and still 404. Legacy Working Together remains available without redirect. Contact stays on the current published consultation destination. Approved missing copy, clean landing/other artwork, logos, principles/recognition, trademark and Contact designs remain dependencies. No next phase was started.

## Final production and hosted verification

- Final no-button intro tested in a separate production-build browser tab: native dialog receives focus, has zero buttons, Escape closes it, overflow clears and focus returns to `landing-statement`. Main wrapper computed opacity is 1. A 402×874 capture verifies vertically stacked words; desktop words spread horizontally.
- Mobile menu restores a scrolled homepage exactly **854px → 854px**; route selection closes it, restores body styles and reaches Work with its existing 69.16px header footprint at 402px. Return Home does not replay the intro.
- Local HTTP: Home, Work and Working Together 200; Our Way and removed fixture route 404; all carry preview noindex. Home HTML contains landing/statement and visible main content, no legacy Mux player or fixture route.
- Application commit `3b62e349a68967d13554f6db298c58c1969ed615` deployed READY as `dpl_FuWWrzzYDbqeiKktQyCZFfVYLV5N`. Hosted browser confirms first-visit words, **zero intro buttons**, automatic completion, refresh bypass, 402px document width, and mobile Menu geometry. Hosted desktop/mobile captures saved; viewport reset afterward.
- Hosted HTTP 200 with `X-Robots-Tag: noindex, nofollow, noarchive`; exactly one absolute preview-origin OG image. `phase5-hosting.json` confirms staging still `dpl_EzsX8AUMtSntpbBQNUrH9Q9WDg6N` and public www still `dpl_2hW8KVM2WdqKzcff48GcNwxsi41B`. No aliases/hooks/CMS writes.
- Final documentation/capture commit contains no application changes. Commits used a one-command signing override; persistent Git configuration is unchanged. Local server and this task’s keep-awake session stopped; hosted preview left open.

# Phase 6 validation — 21 September 2026

## Implementation and automated checks

- `npm run build:redesign`: PASS, 34 static-generation entries including `/our-way`; three published redesign documents fetched. Temporary fixture removed before build and returns 404. New page main content is server-rendered without an admission animation.
- `SANITY_STUDIO_TELEMETRY_DISABLED=1 yarn build` and `node scripts/verify-redesign.cjs`: PASS. Seven existing types, fixed-ID/null/unresolved-reference contracts compile. No CMS writes, schema changes or Studio deployment were needed; authenticated Studio editing remains untested as previously recorded.
- `npm run verify:redesign`: PASS, same 181 legacy-source hashes. Home components/page, Work bodies/fonts, global styles, legacy documents and queries unchanged. Shared Header adds only Our Way hero state.
- `npm run verify:redesign:chrome`: PASS, 31 concrete routes have shared settings; 18 existing image overrides preserved; exactly one absolute OG image and manifest per HTML. Checker now also understands the new Our Way optional SEO image.
- Targeted ESLint (`--no-eslintrc -c .eslintrc.redesign.cjs`, React Hooks and image-alt rules): PASS. Explicit frontend `tsc --noEmit --incremental false`: same **11 inherited errors**; Studio: same **5 inherited errors**. Legacy global lint config remains invalid.
- `npm run check:redesign:release`: expected FAIL with **76 content issues**. No copy/artwork was invented to satisfy the release gate. `git diff --check`: PASS. Exact logs are in `validation/phase6-*`.
- Production-server HTTP: `/our-way`, `/`, `/work`, `/working-together` return 200 with no redirect and preview noindex; removed `/phase6-fixture` is 404. Each has one absolute OG image.

## In-app browser checks

- Only Codex in-app browser used. Six widths **375, 402, 768, 1024, 1512, 1920** have document width equal to viewport width. Hero is 874px at the mobile test height and 982px on desktop. Desktop partnership/credentials use two/three columns; mobile stacks in approved order. Neue Montreal computed on service lists/headings; Baryton on introduction/biography.
- Scroll down moves to introduction at **90px** below the viewport top and focuses `way-introduction`. Header uses white wordmark/transparent white links over hero and existing compact navigation afterward; Our Way has the active state.
- All four published process stages retain CMS order. At 402px the track is 402px wide with 1281px content; the next stage is visible. ArrowRight moves to the following stage (314.5px); native horizontal scroll reaches the final stage at **879px**, its maximum. Physical touch-device gestures were not tested; the control uses native overflow/snap rather than a custom drag handler.
- Mobile Menu opens/focuses Close, Escape restores focus to Menu and clears body lock. Observed scroll restoration was **2200.5 → 2201px**, browser subpixel rounding under 1px. Earlier Home/Work menu behavior is retained.
- Local fixture verifies null page/settings, absent stages/credentials/imagery and omitted booking destination render without crashes or fabricated URLs. Broken hero asset returns 404 and changes to its labelled placeholder.
- Six fixture recognition entries use only supplied Otherness SVGs with explicit fixture labels. Focus pauses animation; persistent Pause remains paused after leaving the strip; Resume becomes running after hover/focus leaves. Duplicate group has zero links and `aria-hidden`.
- Reduced-motion fixture activates the actual generated `prefers-reduced-motion` CSS rules: animation becomes `none`, duplicate group/control hidden, originals wrap statically, document stays 402px. OS-level media emulation is unavailable in this in-app capability; this is stylesheet branch validation, not an OS preference test. Hover pause is implemented with CSS; mouse-hover-only behavior was source checked.
- Local production `/our-way` browser logs contain no errors or warnings. Legacy Work hydration/media issues remain inherited and were not broadened into phase 6.
- Desktop/mobile viewport captures include hero, partnership, process, consultation and desktop credentials. Desktop panel was hidden; screenshots use the browser screenshot API to avoid the native snapshot's clipping.

## Boundaries

This completes phase 6 implementation, not final visual/content acceptance. Clean hero/consultation imagery, four principles, introduction/hero copy, founder note, consultation text, clients/recognition/logos are missing. Final crops/readability and populated editorial lengths need acceptance once supplied. Full published founder biography is deliberately retained, so it is longer than the screenshot draft. Contact designs remain pending for phase 7. `/working-together` redirect stays deferred until launch.

## Hosted phase 6 result

Application commit `e204f364ff487fbf3943cba45b6f487962348c98` deployed READY as `dpl_28UpPgEPpKn48qGc5AUjjvz39rfk`. In-app browser confirms desktop 1512px document width, four process stages, active Our Way navigation, white hero header and one preview-origin OG image. Mobile menu opens, Escape restores focus, and real Our Way → Work → Our Way navigation closes the dialog and clears scroll locks. Hosted `/our-way`, `/work`, `/working-together` return 200/noindex without redirects; removed fixture returns 404. See `phase6-hosting.json`, `phase6-hosted-http.json` and hosted captures. Staging/public production retain the original deployment IDs. No hooks/aliases/CMS mutations. Viewport overrides reset and hosted Our Way left open. Local server and conversation keep-awake session stopped. Documentation commit does not alter application code.

# Home hero QA follow-up — 22 September 2026

## Changes and scope

- Home hero uses one passive scroll listener / animation frame to translate at 35% of scroll distance (65% apparent scroll speed), scale artwork 1→1.06, blur 0→3px and darken the black overlay 0.16→0.30 across one landing height. Media has overscan and clipping; live heading remains sharp. Home sections have an explicit stacking layer above the parallax hero.
- Removed Home's 90%-height compact switch. The oversized logo remains half-cropped at the top with navigation below, including mobile. Home selects the light wordmark over its hero, Services, Results and dark footer area. Interior compact behavior is unchanged.
- Tayte's follow-up request adds a **300ms opacity crossfade** between overlaid light/dark SVG wordmarks. Only one image is exposed to accessibility. Reduced-motion CSS removes the colour transition and hero effects; its runtime branch was source-checked, not OS-emulated in this follow-up.
- Temporary QA mode removes the sessionStorage admission gate and deep-scroll bypass. Intro plays on every Home mount/refresh/return; Escape, missing-pair and reduced-motion bypasses and timer-driven completion remain. Restore once-per-session admission after Tayte accepts testing.
- Live CMS landing artwork now exists. Replaced the verifier's obsolete live-image-must-be-null assertion with a local missing-image diagnostic fixture. No CMS, schema, query, Studio, dependency, Work-body, font-source or global-style edits.

## Validation outcomes

- Targeted ESLint with the isolated config and both Hooks rules: PASS for all changed frontend components/styles and verifier.
- `npm run verify:redesign`: PASS, all 181 preserved legacy-source hashes, fixed IDs/content contracts, missing-image fixture and unresolved-reference diagnostics.
- `npm run build:redesign`: PASS, 34 static entries. Run from an isolated temporary frontend copy with the same source/environment and linked dependencies to avoid disrupting the already-running localhost:3010 development server.
- `npm run verify:redesign:chrome` against that production build: PASS, 31 route shell props and 18 image overrides; one absolute OG image and manifest per HTML.
- Explicit frontend typecheck: same **11 inherited errors**, none in changed files. Saved as `validation/home-hero-qa-typecheck.txt`. Studio checks were not rerun because no Studio/schema/query files changed; prior 5 Studio errors remain inherited.
- Build-time content check now reports **72 editorial/artwork issues**, reduced from 76 because desktop/mobile landing images and alt text are now published. This is not final content acceptance. Legacy global ESLint configuration remains invalid.
- Local Home HTTP 200 with preview noindex. No commit, push, deployment, alias/hook change or CMS mutation in this follow-up.

## In-app browser results

- Used only Codex in-app browser. Hidden panel for desktop captures. Current published sliced hero artwork loads correctly.
- At desktop 1512×982 and scroll 444px: hero top −288.6px, translation +155.4px, next section top 538px, scale 1.02713, blur 1.35642px, overlay 0.223299. Screenshot inspection confirms the next section covers the slower hero without a gap.
- At scroll 1140.5px and 2029.5px: desktop logo stays 1134px wide, top −77.773px / height 155.547px. Navigation remains at y=105.773px; compact is false. Light wordmark treatment was checked on dark Services after the contrast correction.
- At mobile 402×874, after scrolling beyond the hero: logo top −24.273px / height 48.555px / width 354px. Menu remains at y=48.281px, 354×44px. Opening/closing Menu with Escape restores scroll **2622→2622px**, clears overflow and returns focus to Menu.
- Settled resize checks at **375, 402, 768, 1024, 1512, 1920**: document width equals viewport width and logo stays half-cropped. Logo heights were 44.852, 48.555, 54.867, 105.344, 155.547 and 197.516px respectively.
- Crossfade observed in motion at scroll 1805.5px: dark/light opacities **0.195138 / 0.804862**, both transition `opacity 0.3s`, both top −77.773px. Initial white state is light opacity 1 / dark 0 with identical image bounds; no geometric jump.
- Refresh reopens the intro, including a refresh initiated after deep scrolling. Timed completion and Escape were exercised. Real Home→Work→Home navigation completes and replays the intro on return. Work retains its interior header footprint (235.57px at 1920px width).
- No Home warnings/errors in the browser log before the Work navigation smoke check. Existing legacy project hydration/media bugs are outside this follow-up. Physical touch-device performance and OS-level reduced-motion switching were not tested.

## Handoff

Changes remain local on `codex/site-redesign`; the dedicated hosted preview is unchanged. The existing localhost:3010 server remains available for Tayte's testing. Restore the intro session gate only after QA is accepted. Other artwork/copy and Contact designs remain pending. Phase 7/8 have not been started; see HANDOFF.md for the next-phase prompt.


# Header liquid glass follow-up — 22 September 2026

- Active shared Header imports `HeaderGlass.tsx`: native backdrop blur/saturation, an inline SVG edge displacement map, translucent light/dark surfaces, 8px corners, rim highlights and hover/press feedback. Scoped to header links/mobile Menu; footer and mobile dialog links retain their styles. The requested legacy `layout/Header` is unused by Layout and remains untouched.
- This is a CSS/SVG adaptation of https://liquid-glass.ybouane.com/, not installation of its WebGL package. That package requires glass and captured backgrounds to be direct siblings of a positioned root; applying it here would require restructuring/rasterising the page. Native backdrop sampling follows existing scrolling imagery. No dependency or animation loop added. SVG backdrop support varies by browser; standard frosted backdrop and opaque no-backdrop base are provided. Cross-browser rendering is not claimed from the in-app check.
- `npm run build:redesign`: PASS, 34 generated entries, from an isolated temporary frontend copy with linked dependencies (existing localhost:3010 server preserved). Log: `/tmp/otherness-header-glass-build.log`.
- Targeted ESLint with `.eslintrc.redesign.cjs`: PASS for Header and HeaderGlass. `git diff --check`: PASS. Explicit frontend `tsc --noEmit --incremental false`: same 11 inherited errors; no changed-file errors. `npm run verify:redesign`: PASS, including all 181 preserved legacy hashes. No Studio/schema work; Studio checks not rerun.
- In-app browser: desktop 1512×982 Home artwork and scrolled surfaces, Work active state, mobile 402×874 scrolled Menu visually inspected. No horizontal overflow at either width; desktop buttons and mobile Menu remain 44px high. One SVG filter after route navigation; footer backdrop remains `none`.
- Mobile Menu opens/focuses Close; Escape closes, returns focus to Menu, clears body overflow and restores exactly 1964→1964px scroll. Real mobile Home→Work navigation closes the menu. Keyboard Tab reaches Our Way with a visible 2px focus outline. Reduced-motion, reduced-transparency and forced-colour rules are implemented; OS-level preference changes were not emulated. Physical touch and other browser engines remain untested.
- Existing hero QA/intro replay changes are preserved. Existing legacy lint configuration/type/hydration issues and missing editorial/artwork/Contact dependencies remain. No CMS, staging, deployment, push or next-phase work. Next prompt remains the phase 7 Contact dependency prompt in HANDOFF.md, or user-requested visual tuning first.


## Header glass visual correction — 22 September 2026

User supplied a clean rectangular translucent-button reference and requested removal of borders, heavy dark fill and vertical hover movement; subsequent instruction requested substantially lower blur so refraction is visible. Final implementation removes all rim/drop shadows, rounded corners, dark gradients and hover/press transforms. Default tint is white at 8%, hover 16%, pressed 20%. Refraction uses displacement scale 18 with 0.4px blur; standard backdrop fallback uses 2px blur. The filter remains identical across interaction states; only background-color transitions over 180ms, avoiding gradient/background-image swaps. Keyboard focus indication and active-link text underline remain.

- Targeted isolated ESLint on Header/HeaderGlass and `git diff --check`: PASS. `npm run verify:redesign`: PASS, including 181 preserved source hashes. Production build/typecheck were not repeated for this visual adjustment; preceding build and inherited type-error results remain as documented.
- In-app browser at 1512×982: final artwork visible through buttons, computed border 0px, radius 0px, box-shadow none, transform none, tint 0.08, filter `url("#header-glass-refraction") blur(0.4px) saturate(1.05)`.
- Actual pointer entry/exit verified `:hover` true→false, background alpha 0.16→0.08, identical filter, transform none, and background-color-only transition. No gradient or refraction/filter state swap. This is not a cross-engine performance guarantee.
- Home→Work→Home route smoke check passed; active Work has no inset border/shadow. Updated `validation/header-glass-detail.png` replaces the earlier dark bordered treatment capture.
- No deployment/CMS/phase-boundary changes. Existing content/design dependencies and inherited issues remain; next-phase prompt unchanged.

# Hero motion strength and title exit — 22 September 2026

- Increased Home artwork's maximum scale from 1.06 to **1.16** and blur from 3px to **12px**. Existing parallax/darkening curves remain. Title opacity and a separate 0→12px blur follow smoothstep easing over measured logo/title clearance, completing with up to 32px clearance before overlap. Short viewports scale down the clearance/fade distance. A title ResizeObserver also handles wrapping/font layout changes. Once the header arrives, the title stays hidden; reverse scrolling restores it. Reduced motion keeps the title visible and sharp and disables the image effects.
- In-app desktop **1512×982**: at scroll 320px the title is opacity 0.478473, blur 6.25833px, with 109.703px clearance. At scroll 440px it is fully transparent/12px blurred with **25.734px clearance**, before overlap; image scale 1.07169 / blur 5.37678px. Visual inspection confirms a clean handover to the logo and stronger artwork treatment.
- In-app mobile **402×874**: at scroll 320px title opacity 0.245012 / blur 9.05986px; at 440px opacity 0 / blur 12px while logo/title still have **6.594px clearance**. No horizontal overflow. Returning to the top restores opacity 1 and blur 0.
- In-app short desktop **1512×480**: title fully transparent at scroll 120px, still **8.539px clear** of the logo. This verifies the timing adapts to short screens rather than assuming one fixed percentage.
- Targeted ESLint with both React Hooks rules: PASS for HomeLanding. `git diff --check`: PASS. Isolated `npm run build:redesign`: PASS, 34 static entries (log `/tmp/otherness-hero-motion-build.log`). Explicit full frontend typecheck: same 11 inherited errors, none in HomeLanding (`/tmp/otherness-hero-motion-typecheck.log`). No new dependency/schema/Studio work; those checks were not broadened for this motion-only change.
- Existing tab retained earlier Header hot-refresh errors from the parallel glass edits; current full-reload rendering is functional. OS-level reduced-motion and physical-device performance remain untested. This follow-up modifies only HomeLanding and its documentation; independent HeaderGlass changes are preserved. No CMS writes, deployment, push or phase advancement. Intro replay remains enabled for testing. Next work is further requested visual tuning or the existing Contact-design dependency prompt in HANDOFF.md.

# Introduction spacing and typography QA — 22 September 2026

- Added reusable `.heading-small` to the opt-in `redesignScope`, applied to the semantic introduction paragraph. Computed Neue Montreal, normal style, 14px, weight 700, line-height 20.72px, tracking 0.56px and uppercase match the request. The empty CMS statement still renders its labelled placeholder, now in the same typography; final copy wrapping remains pending publication.
- Desktop widths 1024/1512/1920 measure exactly 196px top padding, 64px heading-to-statement, 64px statement-to-button and 128px below button. Paragraph max width 448px. At 375/402/768, retained 96px top/64px bottom, both internal gaps 64px; paragraph widths 327/350/350px. All six document widths equal their viewport width.
- What to expect computed typography: 10px, normal, 700, 12px line-height, 0.4px tracking, uppercase. Padding 12px on all sides, gap 8px, resulting 36px height; inline baseline whitespace removed with vertical-align top. Only the introduction CTA changed. Real mobile CTA click reaches `/our-way`. Desktop/mobile visual inspection passed using only the in-app browser, with the desktop panel hidden. No Home browser warnings/errors observed.
- Targeted ESLint using `.eslintrc.redesign.cjs`: PASS for HomeSections, its styles and scoped tokens. `npm run verify:redesign`: PASS, all 181 preserved legacy-source hashes and existing content contracts. `git diff --check`: PASS.
- Isolated `npm run build:redesign`: PASS, 34 static-generation entries; existing development server preserved. Log: `/tmp/otherness-introduction-build.log`. Explicit frontend typecheck reports the same 11 inherited errors, none in changed files; log `/tmp/otherness-introduction-typecheck.log`. No Studio/schema changes, so Studio checks were not repeated; prior five Studio errors remain inherited.
- No CMS writes, push, deployment or phase advancement. Existing hero/header edits preserved. Missing copy/artwork/Contact designs and intro replay QA override remain inherited dependencies. Next work: user-requested visual QA, or the phase 7 Contact-design dependency prompt in HANDOFF.md.


# Home entrance sequence — 22 September 2026

- HomeIntro now coordinates the existing 4.5s word sequence/0.3s loader fade with three entrance steps: artwork scale 1.04→1 and blur 6→0px for **1500ms**, centred phrase opacity 0→1 and blur 8→0px for **700ms**, then header icon/wordmark/navigation with the same **700ms** reveal. Delays derive from shared duration constants, with no gap between image completion and phrase start. Image easing is cubic-bezier(0.4,0,0.2,1). Total timed sequence is 7700ms; modal/scroll lock releases at completion. Escape/reduced-motion switching finishes immediately and cancels all entrance effects.
- Separate image and phrase wrappers preserve existing scroll transform/filter effects. The phrase wrapper explicitly inherits its h1 font: original Baryton Regular, weight 400 and responsive sizing. Initial wrapper accidentally inherited the scoped sans-serif; this was corrected before final validation. No changes to Header/HeaderGlass were needed.
- In-app desktop 1512×982 verified image → phrase → header ordering before final 1.5s timing adjustment. Final mobile 402×874 sampled sequence: image at scale 1.01374/blur 2.06081px with both text/header opacity 0; next image at scale 1/blur 0 while phrase opacity 0.781498 and header 0; next phrase opacity 1 while header opacity 0.781289. Finished state has all opacities 1, entrance filters/transforms none, no modal/overflow lock, and focus on landing-statement. Computed phrase font is Baryton Regular; document width 402px.
- Escape during the intro immediately reveals all layers, clears overflow and restores statement focus. Mobile scroll at 669.5px retains image scale 1.12256/blur 9.19222px and title opacity 0; entrance filter is none. Mobile Menu opens and focuses Close after completion. OS-level reduced-motion switching and physical touch performance were not tested; bypass/cleanup branches remain source-checked.
- Targeted ESLint with both Hooks rules: PASS (including final timing adjustment). Source preservation: PASS, all 181 legacy hashes. Explicit frontend typecheck: same 11 inherited errors, none in changed files (`/tmp/otherness-hero-entry-typecheck.log`). Isolated production build: PASS, 34 static entries (`/tmp/otherness-hero-entry-build.log`); build preceded the final duration/easing-only adjustment. `git diff --check`: PASS. Existing global lint configuration and Studio/type/hydration issues remain inherited.
- Only HomeIntro, HomeLanding and follow-up documentation changed in this task; all pre-existing workspace edits preserved. No CMS writes, push, deployment, staging changes or new phases. Other artwork/copy and Contact designs remain pending. Next prompt: review entrance timing on localhost:3010 and request further tuning, or use the existing phase 7 Contact-design dependency prompt in HANDOFF.md. Restore session-only intro admission after testing is accepted.

# Services grid, carousel and shared styles QA — 22 September 2026

- Replaced negative-margin accordion positioning with a twelve-column grid. Title starts at column 1 and summary at column 7 (x=64/768 at 1512px). Header and labelled region semantics remain exposed in the in-app accessibility tree despite layout wrappers using `display: contents`. Open item can close; one item opens at a time. Literal `-` and `+` replace the former minus glyph.
- Browser computed styles match: `.type-h3` Baryton Regular 40px/54px, normal/400, -0.4px; `.type-large` Neue Montreal 18px/27px, normal/400, 0.18px; symbol Baryton Regular 48px/64.8px, normal/400, -0.96px. Shared `.button-primary` gives both Services and Home introduction 12px padding, 8px gap, Neue Montreal 10px/12px, normal/700, 0.4px tracking. Description-to-button gap is exactly 32px. Active rule rgb(247,244,240), inactive rgb(103,96,90).
- Native horizontal scroll-snap track reaches the viewport edge. At 1512px, content grid is 1384px, each four-column card is 445.328px and gaps are 24px; fourth card begins at x=1471.984px and extends beyond the viewport. Track client/scroll widths are 1448/1917px. Images retain their varied desktop aspect ratios. Mobile retains the 76% card preview with 24px gaps.
- At 375/402/768/1024/1512/1920px, document width equals viewport width; title/body remain 40/18px, carousel gap 24px, paragraph/button gap 32px. Desktop card widths at 1024/1512/1920 are 282.664/445.328/581.328px. In-app desktop/mobile screenshots visually reviewed with the desktop browser panel hidden.
- Enter closes Branding; Space opens Strategy; clicking Branding closes Strategy. Mobile carousel focused through its labelled list: ArrowRight moves scrollLeft 0→293→586px (max 794px); all four real links remain accessible. Physical touch-device and other browser engines were not tested. CMS consultation link matches the existing published destination; no booking submission performed.
- Targeted ESLint (isolated `.eslintrc.redesign.cjs`, including Hooks rules for components) PASS. `npm run verify:redesign` PASS, all 181 preserved source hashes. Its obsolete requirement that published service artwork remain null was replaced with a local missing-image diagnostic fixture. `git diff --check` PASS.
- Explicit frontend `npx tsc --noEmit --incremental false`: same 11 inherited errors, none in changed files; `/tmp/otherness-services-typecheck.log`. Isolated `npm run build:redesign`: PASS, 34 generated entries; `/tmp/otherness-services-build.log`. Existing localhost:3010 server preserved. No Studio/schema/query changes; previous five Studio errors and invalid global ESLint config remain inherited.
- Existing publication now includes service imagery/copy/captions and introduction copy. Build-time content issues: 38. Remaining Results/Noticed/Our Way artwork/copy, trademark, Contact designs, intro replay acceptance and inherited legacy hydration issues are unchanged dependencies. No CMS mutation, push, deployment or new phase. Other workspace changes preserved. Next prompt: review Services at localhost:3010 and request further visual QA, or use the existing phase 7 Contact-design dependency prompt in HANDOFF.md.

## Services card typography follow-up — 22 September 2026

- Titles now reuse the existing shared `.heading-small` class; removed local typography overrides. Subtitles use new shared redesign `.type-h5`, replacing `.serif` and local desktop/mobile sizes. Image/title spacing changed 16→10px; title/subtitle remains 10px. Mobile bottom padding changed 48→64px, inheriting the desktop rule.
- In-app browser at 375/402/768/1024/1512/1920px: title computes Neue Montreal 14px, normal/700, 20.72px line height, 0.56px tracking, uppercase; subtitle computes Baryton Regular 20px, normal/400, 26px line height, normal tracking. Every width measures image→title 10px, title→subtitle 10px, lowest card content→inside edge of the 1px accordion rule exactly 64px. Document width equals viewport width at every size. Viewport override reset afterward.
- Targeted isolated ESLint PASS; `npm run verify:redesign` PASS including all 181 preserved legacy source hashes; `git diff --check` PASS. Production build/typecheck not repeated for this CSS/class-only follow-up; previous passing build and 11 inherited frontend errors remain documented above. No CMS/schema, deployment, dependencies, Work or phase changes. Existing editorial/Contact dependencies remain. Next prompt: review Services card type/spacing locally or request further QA; phase 7 still requires Contact designs.

# Results fields follow-up — 22 September 2026

- Removed `attribution` from the Home New result object. Remaining result fields: client name, quote, logo, background, optional mobile background. Studio preview uses client title and quote subtitle. Existing CMS documents were not patched; leftover attribution values stay unused and unpublished drafts were not written.
- Results tabs now render `client`. The logo area no longer prints the client name; missing logos use a labelled placeholder only. Background/logo/quote/client name still switch together; keyboard wrap/Home/End, click/tap and no autoplay are unchanged.
- Targeted isolated ESLint (including Hooks rules) PASS for HomeSections. `node studio/scripts/verify-redesign.cjs` PASS. `npm run verify:redesign` PASS, all 181 preserved legacy source hashes. `git diff --check` PASS. Production build/typecheck were not repeated for this field-mapping change.
- Local localhost:3010 HTML: two published results; tabs are `Medable` and `Lorem Ipsum`; Medable panel has logo + quote with no client name beside the logo; the second panel has a logo placeholder without a client name; no attribution copy. Codex in-app browser was not available in this session. No CMS mutation, schema deploy, push, staging change or phase advancement. Next prompt: review Results locally, or use the existing phase 7 Contact-design dependency prompt in HANDOFF.md.

# Results carousel follow-up — 22 September 2026

- Autoplay with a 5s linear underline timeline on the selected tab; completion advances to the next result. Background, logo and quote crossfade with an 8px blur over 1s. Slides are CSS-grid stacked so quote/tab layout does not shift. Logo is 120px × auto; Results heading to copy gap is 320px.
- Clicking a tab or changing it with arrow/Home/End keys stops autoplay and holds that slide (full underline). Autoplay is paused while the section is off-screen and omitted when there is only one result or `prefers-reduced-motion`.
- Targeted isolated ESLint (including Hooks rules) PASS. `git diff --check` PASS. Local localhost:3010 HTML includes two stacked panels, two backgrounds, Medable/Lorem Ipsum tabs, a play timeline on the selected tab, logo class, and served CSS for 120px / 320px / 5s progress / blur. Codex in-app browser was not available, so live timeline/click-to-pause was not interactively timed. No CMS, deploy, push or phase change. Next prompt: review the Results carousel on localhost:3010, or use the existing phase 7 Contact-design dependency prompt in HANDOFF.md.

# Footer styling and bottom reveal — 22 September 2026

- Social buttons compute Neue Montreal 12px, normal/500, 14.4px line-height, 0.96px tracking, uppercase and 8px/16px padding (30.398px rendered height). Both legal phrases match the supplied design; Privacy is un-underlined until hover. Legal gaps are 40px, including the stacked mobile layout. Legal copy is a local presentation override; published settings and social destinations remain untouched.
- White desktop wordmark/nav strip uses fixed bottom positioning below Main and the dark footer. ResizeObserver reserves its exact rendered height; at 1512px both measure 303.546875px. At intermediate scroll the dark footer bottom moves while the strip stays at y=678.453125px in a 982px viewport. Fully covered strip is hidden/inert. Focus into the strip scrolls its reserved space fully into view. Mobile retains the existing omitted strip.
- Shared header opacity follows smoothstep over the dark footer approaching 70%→25% of viewport height. Header becomes inert when fully faded; scrolling up restores opacity 1 and focusability. Reduced-motion uses a discrete hide/show instead; OS-level preference switching was not exercised. Existing hero entrance and glass behavior are preserved.
- In-app browser only: desktop 1512×982 and mobile 402×874 visually inspected; computed no horizontal overflow at 375/402/768/1024/1512/1920. Social spacing/line-height and 40px legal gaps match at all six widths. Home→Work navigation passed, Work footer also fades/reveals, and keyboard Tab from Privacy reaches Otherness home in the white strip; Enter returns Home. No browser warnings/errors observed during the Home/Work smoke check. Full Work filter/media regressions and other browser engines were not rerun. Viewport override reset after testing.
- `npm run build:redesign`: PASS, 34 generated entries; `/tmp/otherness-footer-build.log`. Build preceded final subpixel spacer measurement and focus-scroll refinements, which passed subsequent targeted lint and live browser checks. Build-time published-content diagnostics currently report 34 issues.
- Targeted ESLint using `.eslintrc.redesign.cjs` and both React Hooks rules: PASS for Footer/Header, including final refinements. `npm run verify:redesign`: PASS, including all 181 preserved legacy hashes. `git diff --check`: PASS. Explicit frontend `tsc --noEmit --incremental false`: same 11 inherited errors, none in changed components; `/tmp/otherness-footer-typecheck.log`. No Studio/schema changes, so Studio checks not repeated.
- No prior app listener existed on port 3010; freshly started `npm run dev:redesign` and verified browser access. Local changes only on `codex/site-redesign`; no CMS mutation, push, deployment, staging change or new phase. All pre-existing workspace edits retained. Inherited global lint/type/hydration issues, outstanding artwork/Contact designs and intro replay acceptance remain. Next prompt: “Review the footer styling and reveal at localhost:3010 and tune only the requested details.” Phase 7 remains gated by Contact designs as recorded in HANDOFF.md.

# Mobile menu QA — 23 September 2026

- Reference: `references/Menu Open.jpg`. Transparent native modal preserves the underlying page and existing wordmark; no duplicate logo/social list. Black Close bar replaces the measured Menu position, followed by three translucent centred links. 24px gutters, 8px gaps, Neue Montreal 14/20px bold, minimum 44px targets. Home wordmark-to-button gap reduced from 24px to 12px.
- At scroll 0, links animate upward above the bottom Close bar. At intermediate Home scroll, existing Lenis scrolls to `max(landing height, innerHeight)` over 450ms, then the menu measures the settled header and opens downward. Reduced motion uses immediate scrolling/no link animation. Already-scrolled Home and interior pages open in place. Header updates pause while the modal is open so body locking cannot reset its position.
- In-app browser only: 402×874 start position opens upward (Close y=801.867, navigation bottom=793.867); halfway activation from y=437 settles to y=874 before opening downward (Close/header y=36.281, links y=88.281/140.281/192.281). Escape restores y=874 and Menu focus. At y=0, attempted background scrolling stays locked at 0; Shift+Tab from Close reaches Contact, Tab returns to Close. Close button works. Activation followed by Escape leaves no modal, pending reopen or body lock.
- 375×812 scrolled menu: no overflow, Close y=34.430, navigation bottom=234.430. 768×1024 opens and fits; resizing to 1024px closes/unlocks and restores desktop navigation with no overflow. Mobile Home→Our Work link navigates successfully; Work menu aligns Close and Menu at y=50.555 and closes correctly. Returned Home afterward. No Work body/filter/media changes; full Work regressions not repeated. Browser viewport override reset after testing. Physical iOS/Android, reduced-motion OS switching and reduced-transparency switching were not exercised.
- Read-only geometry/focus/scroll assertions passed for scrolled 402px Home. Reusable browser-console assertions in `validation/mobile-menu-check.js` cover upward/downward placement, completed first-viewport scroll, aligned controls, focus, body lock and overflow; run after opening each position.
- Isolated production `npm run build:redesign`: PASS, 34 generated entries, `/tmp/otherness-mobile-menu-build.log`; built from a temporary frontend copy to keep the QA dev server available. Targeted isolated ESLint with both Hooks rules: PASS for Header/MobileMenu. `npm run verify:redesign`: PASS including all 181 preserved legacy hashes. `git diff --check`: PASS. Explicit `tsc --noEmit --incremental false`: same 11 inherited errors, none in changed components; `/tmp/otherness-mobile-menu-typecheck.log`. No Studio/schema changes, so Studio checks not repeated.
- Local only on `codex/site-redesign`; no CMS writes, push, deployment or phase advancement. Prior workspace changes retained. Existing lint/type/hydration issues, artwork/copy acceptance, Contact designs and temporary intro replay remain inherited dependencies. Next prompt: “Continue Home mobile QA at localhost:3010; review the mobile menu and apply only the next requested adjustment.”

# Mobile menu exit and glass follow-up — 23 September 2026

- Reused `headerGlass` on each mobile navigation link: 8% white idle tint, hover/press tint changes, SVG refraction + 1px blur/1.05 saturation in the in-app browser, standard 2px blur fallback, existing reduced-transparency/forced-colors rules. Removed the previous 80% light fill/8px blur override. Close stays black.
- Native Web Animations API fades the links out and moves them 8px toward the trigger over 200ms, sampling their current entrance position for quick reversals. The native modal, scroll lock and focus remain active until completion. Close, Escape and ordinary link selection use this path; link navigation follows successful completion. Modified clicks retain browser behavior. Repeated close requests reuse the pending animation; cleanup cancels it and prevents cancelled navigation. Route changes initiated elsewhere and desktop resize still clean up immediately. Reduced motion bypasses the exit.
- In-app browser at 402×874: glass visibly refracts hero artwork and scrolled page text. Computed styles on all three links confirm rgba(255,255,255,0.08) and the shared SVG filter + blur/saturation. Close remains modal/scroll-locked immediately after activation, then closes/unlocks and returns Menu focus; repeated open→Escape→reopen succeeds. Scrolled Home→Our Work navigation completes after closing; body is restored. Previous upward/downward placement retained. Returned Home and reset viewport override. Physical mobile browsers and OS motion/transparency preference switching remain untested.
- Targeted isolated ESLint with both Hooks rules: PASS (MobileMenu/Navigation). `npm run verify:redesign`: PASS, including all 181 preservation hashes. `git diff --check`: PASS. Typecheck still reports 11 inherited errors, none in these components (`/tmp/otherness-menu-exit-typecheck.log`). Previous passing production build remains applicable as a baseline; a new full build was not run for this focused follow-up. No Studio/schema/CMS/deployment/phase changes.
- Existing artwork, Contact designs, intro replay acceptance and inherited global lint/type issues remain. Next prompt: “Continue mobile Home QA at localhost:3010 and tune only the next requested detail.”

# Mobile menu glass removal — 23 September 2026

- Removed shared liquid-glass styling from mobile menu links only after Tayte reported entrance jank. Links now have a plain rgba(235,232,229,0.8) fill and no backdrop filters; reduced-transparency uses a solid fill. Black Close, opening/200ms exit animations, scroll sequencing and header glass remain unchanged.
- In-app 402×874 check: all three menu links compute the expected translucent fill and `backdrop-filter: none`; menu opens and Close responds. Viewport reset. Targeted isolated ESLint with Hooks rules and `git diff --check` PASS. Existing menu assertion updated to require no backdrop filter. Build/typecheck not repeated for this CSS/import-only adjustment; earlier build and inherited 11 type errors remain recorded above.
- Local only, no CMS/deploy/phase changes. Existing artwork/Contact dependencies and intro replay acceptance remain. Next: continue Tayte’s mobile Home QA.

# Mobile Results spacing, looping tabs and swipe — 23 September 2026

- Reference: `references/results-mobile.jpg`. Removed mobile `min-height: 760px`; inactive panels are positioned out of flow so only the active quote determines height. Mobile quote margin is 0 and tab rail margin-top is 40px. Desktop keeps stacked panels and its original tab layout.
- Mobile selected tab/rule always occupies the original centred 24px inset, spanning viewport minus 48px. CSS order cycles remaining clients to the right with a 12px gap. The tab rail has visible overflow and 24px negative side margins; `overflow-x: clip` belongs to the full-width Results section, so neighboring details extend to the screen edge. No early inset masking and no horizontal page overflow.
- Primary horizontal pointer swipes of at least 50px, dominant over vertical displacement, select next/previous with modulo wrap. Native `pan-y pinch-zoom` remains enabled; vertical drags do not select a result. Non-primary pointer release is ignored. Swipe-generated clicks are suppressed, and focus moves out of an outgoing focused panel to its new active tab. Click/keyboard/swipe selection pauses autoplay; the existing 5s autoplay and 1s crossfade remain.
- In-app browser checks: at 402×874 both published quotes measure exactly 40px quote-bottom→tab rule. Active tab x=24..378, next tab x=390..744; next details remain visible through x=402. Clicking the visible next-tab edge centres Lorem Ipsum at x=24 while Medable cycles to x=390. Left swipe wraps last→first; right swipe wraps first→last; ArrowRight wraps last→first. Subsequent swipe confirms focus is on the new active tab, outside aria-hidden content. Vertical drag retains selection. User selection sets data-playing=false.
- 375px: active tab x=24..351, gap 40px, section x=0..375 and document width 375. 768px: active tab x=24..744, gap 40px, document width 768. At 1512px, both panels remain static in grid-area 1/1, both tab orders are 0, rail overflow is auto and document width is 1512. Viewport override reset afterward. These are in-app pointer-drag checks; physical iOS/Android swipes and multi-touch gestures were not exercised. No new multi-slide/null fixtures or autoplay timing run; published two-slide content and existing empty/single-slide guards were inspected.
- `npm run build:redesign` in an isolated temporary frontend copy: PASS, 34 generated entries (`/tmp/otherness-results-mobile-build.log`), keeping the QA dev server running. Build preceded only the final non-primary pointer-release guard; final targeted lint covers that guard. Targeted isolated ESLint with both Hooks rules: PASS. `npm run verify:redesign`: PASS, all 181 preservation hashes. Typecheck: same 11 inherited errors, none in HomeSections (`/tmp/otherness-results-mobile-typecheck.log`). `git diff --check`: PASS. Runnable DOM checks: `validation/results-mobile-check.js`.
- No CMS/schema/content changes, push, deployment, staging changes or phase advancement. All other workspace edits preserved. Existing content/artwork/Contact dependencies, intro replay acceptance and inherited lint/type issues remain. Next prompt: “Continue Home mobile QA at localhost:3010; review Results spacing/swipes and apply only the next requested detail.”

# Results layout stability — 23 September 2026

- Replaced mobile active-only height with the existing shared grid’s tallest-content sizing. Every panel participates in the same grid cell; quote `margin-top: auto` pins its bottom while logos stay at the same top position. No arbitrary large min-height is required. Tabs retain the 40px gap and viewport-edge overflow; looping/swipe logic is unchanged. Logo width/height attributes use already-projected metadata to reserve the image aspect ratio before loading.
- In-app before/after selection checks pass for both published quotes: at 402px section height=948.53125px, tab offset=840.53125px; at 375px height=985.96875px, tab offset=877.96875px; at 768px height=761.34375px, tab offset=653.34375px. Each pair is identical across slide changes. In all cases logo top offset=379.59375px, quote→rule gap=40px, and following Noticed offset equals unchanged section height. Viewport reset afterward. Physical device testing was not repeated.
- Targeted isolated ESLint with Hooks rules and `git diff --check`: PASS. Prior production build and same 11 inherited type errors remain the baseline; not repeated for this CSS/intrinsic-image-size follow-up. Updated the reusable mobile DOM check to assert equal quote-bottom positions across all slides.
- No CMS, schema, deployment, dependencies, Work-body or phase changes. Existing editorial/Contact/intro acceptance dependencies remain. Next: continue Tayte’s mobile Home QA.

# Mobile Noticed and footer spacing — 23 September 2026

- Mobile-only accordion markup keeps desktop links/cursor thumbnails intact. First entry starts open; title buttons toggle one panel or close all. Arrow is an independent anchor; external URLs use `_blank`/`noreferrer`, existing project references retain their internal destinations. Content uses two equal columns with 18px gap and 168:114 thumbnail ratio. Real long titles wrap, unlike the repeated short placeholder in the reference.
- Reuses Services’ native height helper: 550ms cubic-bezier expansion then 500ms opacity/6px blur entrance; exit fades first then collapses. Initial mount and reduced motion are immediate. Closed regions are inert. A quick close→open before height collapse exposed a missing transitionend; shared helper now releases to auto immediately when already at target height. Hidden desktop mobile panels also initialize with auto height. No new dependencies.
- In-app browser: first row opens after reload; clicking Flydog closes first/opens second. During transition, outgoing panel retained 164px height while opacity fell to 0.0058 and incoming panel expanded to 141.35px while opacity stayed 0 (before entrance delay). Settled closed panel is zero/inert; open panel returns to auto. Enter/Space expand/collapse. Rapid Space close→open leaves auto height and resizes correctly from 402px to 768px, panel/body both 262.171875px at 768px. Final first panel/body both 144px at 402px. No document overflow at 375/402/768/1512px. Desktop mobile markup is display:none, desktop link grid and 40px legal gaps remain.
- Arrow activation leaves the accordion state unchanged. The external Flydog anchor’s exact CMS href, `_blank` and `noreferrer` were verified. The in-app browser did not expose a new destination tab after activation, so external destination loading was not verified. Physical mobile devices, OS reduced-motion switching, empty/new content fixtures and full Work regressions were not exercised.
- Mobile footer measured at 375/402/768px: top 64px, icon→heading 96px, booking→socials 96px, socials→legal 64px, legal gap 16px, bottom 24px. Mobile Noticed and footer screenshots visually inspected. Viewport reset afterward.
- Isolated production build PASS, 34 entries (`/tmp/otherness-noticed-build.log`); build preceded the final already-at-target height guard. Final targeted isolated ESLint with Hooks rules and `git diff --check` PASS. Source/content preservation PASS, all 181 hashes (`/tmp/otherness-noticed-verify.log`). Explicit typecheck: same 11 inherited frontend errors, none in changed files (`/tmp/otherness-noticed-typecheck.log`). Reusable settled-state assertions: `validation/noticed-mobile-check.js`.
- Local only; no CMS/schema, deployment, push, staging or phase changes. Other workspace edits retained. Existing global lint/type/hydration issues, Contact designs/artwork acceptance and temporary intro replay remain. Next prompt: “Continue Home mobile QA at localhost:3010 and apply only the next requested adjustment.”

# Mobile bottom footer reveal — 23 September 2026

- Removed only the mobile `display:none` rule for `.footer-reveal` and `.footer-strip`. Existing fixed-bottom positioning, measured spacer, visibility/inert observer and focus-scroll behavior now apply on mobile. Dark-footer spacing and 16px mobile legal gaps remain intact; no new script or assets.
- In-app browser: at 402×874, white strip and spacer both 189.3515625px, fixed top 684.6484375/bottom 874. Reverse scroll moves dark footer bottom from 684.398 to 815.398px while strip stays fixed; further reverse scroll hides strip and makes links inert. Fully revealed at 375×812: strip/spacer 186.578125px, bottom 812; at 768×1024: strip/spacer 227.0078125px, bottom 1024. No horizontal overflow at all three widths, legal gap remains 16px. Screenshot visually reviewed at 402px; viewport reset. Physical devices and actual navigation were not rerun for this CSS-only restoration.
- Targeted isolated Footer ESLint including Hooks rules and `git diff --check`: PASS. Build/typecheck not repeated for removal of one CSS rule; prior build and 11 inherited type errors remain the baseline. No CMS/schema, deployment, push, dependencies, Work-body or phase changes. Next: continue requested Home QA; prior editorial/Contact/intro acceptance dependencies remain.

# Hero entrance head start — 23 September 2026

- Changed only the image entrance offset from 0 to -200ms: starts at 4.6s, ends at 6.1s. Loader fade ends at 4.8s; phrase/header remain at 6.3s/7s and completion at 7.7s. Existing keyframes, duration, easing, cancellation and reduced-motion bypass remain.
- Targeted isolated HomeIntro ESLint with Hooks rules and `git diff --check`: PASS. In-app localhost:3010 reload shows the loader, then the completed home page with heading focus restored. Browser read-only API does not expose `getAnimations`, so exact live animation timing was not instrumented; timing verified in source. Build/typecheck not repeated for this numeric-only adjustment; prior build and 11 inherited type errors remain the baseline.
- Local only; no CMS/deployment/phase changes. Existing artwork/Contact/intro acceptance dependencies remain. Next: continue requested Home QA.

# Expanded shared header — 23 September 2026

- Removed compact state, non-home scrollY > 100 trigger, data attribute and desktop/mobile hiding/translation styles. Layout uses `redesign/Header.tsx`; legacy `layout/Header` is unused and retained. All shared-header routes inherit the change. Home choreography, colour changes, footprint and footer fade remain.
- In-app Our Way at desktop scrollY=632 and 402×874 mobile scrollY=720: logo visibility is `visible`, header-inner transform is `none`; mobile Menu displays normally. Viewport override reset. Targeted Header ESLint including Hooks rules and `git diff --check` PASS. Full build/typecheck and every route were not rerun for this removal; prior build and 11 inherited type errors remain the baseline.
- Local only, no CMS/deployment/phase changes. Existing editorial/Contact/intro acceptance dependencies remain. Next: continue requested visual QA.

# Our Way credentials QA — 23 September 2026

- Used existing authenticated Sanity client to patch only `ourWayPage.credentials.services`, `.clients`, and `.recognition`, guarded by the fetched revision. No draft existed. Seeded 16/14/11 typed, keyed entries in screenshot order; no links. CreativeBoom appears twice exactly as supplied. Backup: `/tmp/otherness-our-way-before.json`; verified result: `/tmp/otherness-our-way-seeded.json`.
- Fresh post-write assertions PASS: exact list content, all other singleton fields (including five logo objects) unchanged, all 656 other document IDs/revisions unchanged. No schema changes or asset uploads.
- Removed recognition Pause/Resume button, React state, data attribute and unused control styles. Existing hover/focus pause and reduced-motion static layout retained.
- In-app `localhost:3010/our-way`: rendered counts 16/14/11, zero list links, zero movement buttons, all five original logos loaded after scrolling, recognition-drift animation present, no document overflow at the current viewport. No viewport override. Responsive matrix and OS reduced-motion switching were not repeated.
- Targeted isolated ESLint including Hooks rules PASS; `npm run verify:redesign` PASS with all 181 legacy preservation hashes; `git diff --check` PASS. Full build/typechecks not repeated for this removal/content patch; inherited 11 frontend and 5 Studio errors, invalid global lint config and legacy hydration issues remain the prior baseline.
- CMS content published; frontend change local only. No push, deployment, staging changes or phase advancement. Remaining editorial/artwork acceptance, temporary Home intro replay and Contact designs remain. Next prompt: “Continue Our Way QA at localhost:3010 and apply the next requested adjustment.”

# Our Way hero motion — 23 September 2026

- Removed Scroll down markup/styles. Separate media/reveal layers avoid entrance transforms fighting scroll transforms. Native animations run sequentially: scale 1.04→1 and blur 6→0px for 1500ms, then height 100vh→calc(100vh - 32px) for 800ms. Scroll effect becomes active after completion. It uses Home’s 35% translation, up to 16% zoom, 12px blur and +0.14 shade opacity, preserving Our Way’s existing 0.22 base shade. Following opaque sections cover the hero; process/consultation colours preserved.
- Reduced-motion preference bypasses/cancels entrance and disables scroll effects; restored nonzero scroll bypasses entrance. Effects clean up animations, listeners, resize observer and RAF on unmount. No scroll locking. Existing crop, mobile sources, image error fallback and heading typography retained.
- In-app browser: initial desktop observed height=941/viewport=941, blur=5.40px/scale=1.036 during reveal; settled height=909. At scrollY=343, hero top=-222.95, intro top=566, image blur=4.52805px/scale=1.06037. Dark/taupe section backgrounds preserved.
- Mobile 402×874 sampled sequence: reveal height 874/blur6 → recede height874/blur0 → intermediate heights decreasing smoothly → settled842/blur none. Screenshot visually reviewed with 32px paper strip. Settled mobile scroll assertion PASS (scrollY3882.5, capped translation294.7, blur12). Desktop 1512×982 assertion/screenshot PASS: hero950, next section top950, no overflow. Reusable assertions: `validation/our-way-hero-check.js`.
- Browser viewport/visibility batch initially timed out after applying mobile size; reconnected successfully and completed checks. No physical-device, OS reduced-motion switching or actual restored-scroll test completed (browser reload reset to top). These branches were source-reviewed only.
- Isolated production `npm run build:redesign`: PASS (`/tmp/otherness-way-hero-build.log`). Targeted isolated ESLint with Hooks rules and `git diff --check`: PASS. `npm run verify:redesign`: PASS, all 181 legacy hashes. Explicit typecheck: same 11 inherited errors, none in Our Way (`/tmp/otherness-way-hero-typecheck.txt`). Existing Studio errors/global lint/hydration baseline remains.
- Local only: no CMS, deployment, dependency, Work-body or phase changes. Existing Contact/editorial/artwork acceptance and Home intro replay dependency remain. Next prompt: “Continue Our Way hero QA at localhost:3010 and apply the next requested refinement.”

## Our Way sentence follow-up in the same QA task

- Added an independent statement span with a 1000ms entrance delay and 700ms opacity0→1/blur8→0px animation, using Home’s entrance easing. Scroll opacity/blur acts on the parent heading so the effects do not conflict. Fade distance derives from actual title height/header bottom; smoothstep easing finishes before the title reaches the header and reverses when scrolling up. Title ResizeObserver handles text/font wrapping changes.
- In-app live samples: sentence opacity0 at119ms; opacity0.104 at1061ms; opacity1 at1750ms while hero recedes. At scrollY376: title top172.307/header bottom119.789, opacity0.0443/blur11.469px. Reverse to scrollY0 restores opacity1/blur0. Desktop/mobile hero height checks from above remain valid. Final viewport override reset and preview shown.
- Final production rebuild and targeted lint pass. Same inherited type errors remain; no new dependencies or CMS writes.

# Our Way process content — 23 September 2026

- Revision-guarded patch of published `ourWayPage.process.heading` and `.stages`; no draft existed. Four titles and service lists (4/4/4/5 items) match the supplied design; heading is “Our process together”. No links added. Existing introduction and stage keys preserved.
- Fresh uncached Sanity assertions PASS: exact process content, all other singleton fields unchanged, all 656 other document IDs/revisions unchanged. Backup `/tmp/otherness-process-before.json`; verified result `/tmp/otherness-process-after.json`.
- Data-only update: no frontend/schema edits, build, browser checks, deployment, staging changes or phase advancement. Existing frontend/Studio type and lint issues and Contact/editorial/intro acceptance dependencies remain unchanged. Next prompt: “Continue Our Way QA and apply the next requested adjustment.”

# Our Way scroll, typography and header QA — 23 September 2026

- Diagnosed `.process-track[data-lenis-prevent]`: installed Lenis 1.0.39 returns before consuming wheel events over that subtree, allowing native scrolling while an earlier Lenis animation can still run. Replaced with the existing Home track convention `data-lenis-prevent-touch`; vertical wheel input stays under Lenis, pure horizontal wheel/native touch retain the overflow track. No global scrolling options changed.
- Header previously switched Our Way dark immediately after the hero and never returned to white over process/consultation. Added explicit light/dark section markers and independent visible-midpoint measurements for wordmark and desktop nav/mobile Menu. Reverse document order gives opaque sections precedence over the translated hero. Hero resize is observed, including entrance height recession. Existing footer fade and menu-open guard remain.
- Shared-header active link is opaque white/dark ink over dark content, inverse over light content, with no underline, shadow or refraction. Inactive glass remains. Colour/fill transition is 300ms, matching wordmark opacity; reduced-motion and forced-colour overrides retained. Work active state also verified.
- Process and credentials entries reuse `.type-large` directly on each list item, fixing inherited weight 500 and removing 14/16px overrides. All 58 entries compute Neue Montreal, normal/400, 18px/27px and 0.18px tracking at 375/402/768/1024/1512/1920px; no horizontal document overflow at any width.
- In-app browser only: desktop 1512×982 and mobile 402×874 swept hero, introduction, partnership, process, consultation, credentials, recognition and dark footer. Correct light/dark state assertions passed throughout. Desktop hero boundary at scroll882: white wordmark over hero, dark navigation over introduction. Process boundary at scroll2126: dark wordmark over partnership, light navigation over process. Mobile scroll2562 similarly splits partnership/process. At desktop scroll2326/2588.5/2386 and mobile2771 the process header is white; consultation is white; credentials/recognition return to dark. Footer opacity reaches0/inert as before, and restores on return to top.
- Desktop forward/reverse wheel scrolling over process lists exercised; mobile horizontal scroll moved track0→314.5px while page stayed at2562px, then vertical wheel moved page to2771px while retaining track314.5px. Mobile Menu opens/focuses Close, Escape closes and returns focus to Menu. These are in-app wheel/keyboard checks, not physical touch or frame-time profiling. OS-level reduced-motion, reduced-transparency and forced-colour switching were source-reviewed only.
- Desktop hero/process and mobile boundary screenshots visually inspected. Browser viewport override reset. Work route smoke check confirms solid dark active Our Work with white text/no underline; actual primary-link navigation returns to Our Way. Local Our Way left open for review. A first route smoke attempt navigated back before the asynchronous route completed; corrected by awaiting the actual route/selector. No application failure inferred from that automation timing issue.
- Reusable read-only browser assertions: `validation/our-way-scroll-check.js` (run after scrolling with menu closed). Targeted isolated ESLint with both Hooks rules and `git diff --check`: PASS. Isolated `npm run build:redesign`: PASS (`/tmp/otherness-way-qa-build.log`); `npm run verify:redesign`: PASS, all 181 legacy preservation hashes. `tsc --noEmit --incremental false`: same 11 inherited frontend errors, none in changed files (`/tmp/otherness-way-qa-typecheck.log`). Studio unchanged; its prior five errors, invalid global lint configuration and legacy project hydration issues remain inherited.
- Local only: no CMS writes, dependency changes, deployment, push, staging changes, Work-body edits or phase advancement. Existing Contact/editorial/artwork acceptance and temporary Home intro replay remain dependencies. Next prompt: “Continue Our Way QA at localhost:3010 and apply the next requested adjustment.”

# Our Way mobile header crop and process scrollbar — 23 September 2026

- Root cause: shared interior mobile `.logo-row` overrode the proportional crop with fixed `-10px`; Home separately calculates half the scaled logo height. Replaced that offset with `calc(min(100vw - 48px, 400px) * -31 / 226 / 2)`. Home overrides and desktop rules remain unchanged.
- Mobile process track uses `scrollbar-width: none` and `::-webkit-scrollbar { display: none; }`, matching Home's project-track pattern. Native overflow, Lenis touch handling, focusability and snap rules retained.
- In-app Our Way geometry: 375px logo top -22.421875/height 44.8515625; 402px top -24.2734375/height 48.5546875; 768px top -27.4296875/height 54.8671875. Lower half visible within subpixel rounding, no document overflow. At 402px Menu starts at 36.28125, preserving exactly 12px gap. Desktop 1512px top -77.7734375/height 155.546875 (exactly half).
- At 402px process computed overflow-x=auto, scrollbar-width=none, WebKit scrollbar display=none. Keyboard ArrowRight moves the focused track; horizontal wheel moves to 628.5px while page stays at 2426.5px. Header and process screenshots visually reviewed. Menu opens with Close focused; Our Work navigation succeeds and its logo matches Our Way. Home's settled header also matches exactly at 402px. Browser viewport override reset after testing. Physical touch devices and OS accessibility preferences were not retested.
- Targeted ESLint with isolated redesign config, browser globals and both React Hooks rules: PASS. `npm run verify:redesign`: PASS, all 181 legacy preservation hashes. `git diff --check`: PASS. Full build/typecheck not repeated for this CSS-only patch; prior passing build and 11 inherited frontend type errors remain the baseline, alongside existing Studio/lint/hydration issues.
- Local only, no CMS/schema/deployment/push/Work-body/phase changes. Existing Contact/editorial/artwork acceptance and Home intro replay dependencies remain. Next prompt: “Continue Our Way mobile QA at localhost:3010 and apply the next requested adjustment.”

# Phase 7 Contact / Cal.com — 23 September 2026

- Input: supplied `Contact.jpg` and `Logistics Discussion.rtf`. Implemented desktop composition, responsive mobile adaptation, `/contact` SEO and published shared shell props. No form/backend or new CMS schema/document. Explicit hardcoding request supersedes earlier Contact singleton planning. Shared navigation switches Contact to `/contact`; Contact’s compact white footer excludes the large CTA/reveal and does not fade the header. Existing published social destinations retained.
- `ConsultationLink.tsx` shares the supplied HTML embed through Next Script, namespace `discovery`, `otherness/discovery`, month view, small-screen slots and visible event details. Home’s five service links, Home footer, Our Way consultation and footer, Contact CTA and other shared footer CTAs use it. Direct href works before/without the SDK; modifier clicks retain ordinary links. No dependency/lockfile, CMS or Work-body changes.
- Added dialog semantics, inert background, focus on Close/return to trigger, Lenis pause/resume, body overflow restoration, Escape during loading and route cleanup. The SDK creates another modal if reopened before readiness; the observer handles the latest modal as well as SDK reuse. Closed modals are inert. Direct booking fallback lives in the vendor shadow root outside its transformed calendar container.
- In-app checks: desktop 1512×982 and mobile 402×874 Contact screenshots visually inspected. Width matrix 375/402/768/1024/1512/1920 has matching document/viewport widths, contained heading/button and no horizontal overflow. Desktop footer is 100px high at y882 of 982; mobile footer stacks at y680 with natural page scrolling. Final desktop heading tuned to 6vw and button y635, matching supplied reference proportions. No mobile Contact reference was supplied.
- Contact mouse/Enter opening, Escape/Close and focus/scroll/inert restoration PASS. Mobile Menu opens/closes, points Contact to `/contact`, and actual Our Way navigation passes. Our Way section/footer and Home Branding service/footer each open and dismiss the correct modal; every rendered Home service trigger points to the same event. Repeated opening across section/footer triggers exercises new-modal handling. Read-only reusable assertions: `validation/contact-check.js`.
- **Embedded calendar loading NOT verified / acceptance blocker:** the supplied SDK loads, creates the modal and correct `https://app.cal.com/otherness/discovery/embed?...` iframe, but in this in-app browser the child stays at `about:blank` and SDK state stays `loading`. Reproduced independently with a temporary plain iframe using both app.cal.com and cal.com; test route and diagnostic style removed. No source/network-policy bypass used. The exact embed endpoint returns HTTP 200; both standalone event and standalone embed URL render the real “30 minute discovery call”, Studio Otherness, 30m, Cal Video, Requires confirmation and available September dates/times. Native AX click on the modal’s fallback navigates successfully to the event. No appointment or personal data submitted. These observations do not prove the root cause or an end-to-end booking inside the modal; keep phase 7 acceptance open.
- Isolated production `npm run build:redesign` PASS (`/tmp/otherness-contact-build.log`), including `/contact`. Targeted isolated ESLint with both React Hooks rules PASS. `tsc --noEmit --incremental false` remains the same 11 inherited frontend errors (`/tmp/otherness-contact-types.log`), none in changed files. `npm run verify:redesign` PASS: all 181 legacy preservation hashes. `git diff --check` PASS. Studio unchanged, not rebuilt; prior five Studio errors, invalid global lint config and legacy project hydration issues remain.
- Development cache became stale during route/hot reload tests (`getStaticPaths is not a function`, missing manifest entries and Next FOUC hiding). Restarted the verified 3010 server and moved generated `.next` to `/tmp/otherness-contact-stale-next-20260923`; a clean dev session restored pages and all subsequent checks above. Did not change app rendering to mask those cache errors. Build ran in an isolated `/tmp/otherness-contact-build.*` copy, leaving development outputs separate.
- Local only on `codex/site-redesign`: no commit/push/deploy, staging/production changes or CMS mutations. Existing user changes preserved. Remaining: modal calendar loading, Contact visual acceptance, final editorial/artwork acceptance and temporary Home intro replay. Next prompt is in HANDOFF.md; phase 8 not started.


# Work listing CTA removal — 23 September 2026

- Tayte explicitly requested removal of “Transform your complexities into strengths.” from `/work`, superseding Work-body preservation only for this change. `ProjectsList` now maps the same ordered data into one gallery grid. Removed the CTA props/import, duplicate wrappers/maps and already-commented Load More markup. The existing pagination/filter functions, card component, fonts, shared CTA component and CMS data/schema/query remain unchanged.
- In-app responsive checks at 375/402/768/1024/1512/1920px: 16 project links in identical order, one gallery grid, zero `.cta-banner` elements and document width equal to viewport width. The gap between cards 6 and 7 is exactly 32px at the first three widths and 64px at the last three, matching the grid’s normal row gap. Mobile is one column; desktop two columns.
- Visually reviewed desktop cards and mobile Narro → Confidential seam. Tab from Narro reaches Confidential directly. Clicking Artsy returns Famille Elastique, Harper, Confidential and Saima Zaidi. No booking or CMS mutation. Browser viewport override reset after QA.
- `npm run build:redesign` PASS in isolated `/tmp/otherness-work-cta-build.*`, log `/tmp/otherness-work-cta-build.log`. Targeted `eslint --no-eslintrc --config .eslintrc.redesign.cjs components/blocks/ProjectsList/ProjectsList.tsx` PASS. `tsc --noEmit --incremental false` remains the same 11 inherited errors, none in changed files (`/tmp/otherness-work-cta-types.log`). `git diff --check` PASS.
- `npm run verify:redesign` PASS: 181 hashes checked, comprising 179 unchanged legacy hashes and two explicitly pinned approved CTA-removal hashes (ProjectsList and Work index after shell-prop normalization). Original baseline file retained. Existing data assertions also pass. Studio unchanged/not rebuilt; inherited global lint, Studio type and project hydration issues remain.
- Local only on `codex/site-redesign`, no commit/push/deployment/staging/CMS/asset changes or phase advancement. Remaining dependencies: Contact embedded calendar acceptance, editorial/artwork approval and temporary Home intro replay. Next prompt: “Continue Work QA at localhost:3010/work and apply the next requested adjustment.”


# Home earlier fades / Our Way scroll cue — 23 September 2026

- Home: shared `REVEAL_START = IMAGE_DURATION - 500` advances the sentence from 6.3s to 5.8s and header from 7s to 6.5s. Both retain 700ms blur/fades. Intro completion/focus/scroll release moves from 7.7s to 7.2s to match the final fade. Image still starts at 4.6s and lasts 1.5s. Reduced-motion/Escape paths unchanged.
- Our Way’s requested behavior already exists: 1.5s image entrance, then 800ms shrink from `100vh` to `calc(100vh - 32px)`. In-app browser observed mobile reveal at 941px high, then settled at 909px with the next section at y909 and scroll 0; screenshot visibly confirms the 32px paper strip. Desktop 1512×982 settles at 950px with next section at y950. No Our Way source edit required. Viewport override reset.
- Targeted HomeIntro isolated ESLint with browser globals and both React Hooks rules PASS. `git diff --check` PASS. Home reload completes, displaying the sentence/header and returning focus to its heading. Animation-object introspection is unavailable in the browser’s read-only DOM API, so exact Home timestamps were verified from the shared timing constants, not claimed as recorded frames.
- No full build/typecheck repeated for this timing-only patch; the immediately preceding Work production build passed and 11 inherited frontend type errors remain the baseline. Existing Studio/global lint/project hydration issues and Contact embedded-calendar/editorial/artwork/Home intro replay dependencies unchanged.
- Local only, no CMS/asset/deployment/staging changes or phase advancement. Next prompt: “Continue hero animation QA at localhost:3010 and apply the next requested adjustment.”


# Staging promotion — 23 September 2026

- Tayte explicitly authorized pushing the current redesign to `staging`; this overrides the previous isolation restriction for this staging promotion only. Fresh fetch confirms `origin/staging` has no divergent commits and can fast-forward from `2707efa`. Master/production remains unchanged.
- Final isolated `npm run build:redesign` PASS (`/tmp/otherness-staging-build.log`), including Home, Our Way, Contact and Work. `npm run verify:redesign` PASS, all 181 expected hashes and published-data assertions. Staged diff whitespace check PASS after normalizing trailing console whitespace in the saved Home build log. The preceding explicit frontend typecheck still has 11 inherited errors; build intentionally skips type/lint enforcement.
- Includes current application refinements and their documented validation/design references. Unused original Neue Montreal OTF/TTF sources and generated `tsconfig.tsbuildinfo` remain local/untracked; the app uses the already-committed WOFF2 files. No CMS mutation or Studio deployment performed by this promotion.
- Remaining acceptance: embedded Cal calendar loading in the in-app browser, final editorial/artwork review and temporary Home intro replay. These remain staging review items; production launch is not authorized by this request.

# Home intro testing mode ended — 23 September 2026

- Tayte confirmed once-per-browser-tab playback. Restored the original session-storage gate (`otherness:intro:v1`), marking admission before playback. Refresh/return visits skip it; unavailable storage, reduced motion, missing pairs and restored scroll >20px bypass it. Existing 7.2s entrance choreography, timer cleanup and Escape behavior retained.
- Codex in-app browser at localhost:3010: first visit shows the welcome dialog; refreshing during playback skips it; real Home → Our Way → Home navigation skips it with document overflow restored. A separate tab independently shows the welcome dialog and then completes with no dialog/scroll lock and `landing-statement` focused. PASS.
- Targeted ESLint using the isolated redesign config, browser environment and both React Hooks rules: PASS. `git diff --check`: PASS. Full build/typecheck not repeated for this restored admission block; existing 11 frontend and five Studio type errors, invalid global lint config and legacy hydration issues remain inherited. Reduced-motion/storage-failure/restored-scroll branches were source-reviewed, not newly browser-emulated.
- Local only; no CMS, deployment, staging or phase change. Intro-replay dependency resolved. Contact embedded calendar and editorial/artwork acceptance remain. Next requested follow-up: investigate and smooth page/header/hero transitions.

# Shared page/header/hero transitions — 23 September 2026

- Root causes: Our Way/Contact lacked page motion wrappers, Home suppressed entrance animation, and the header/footer lived outside route presence and switched routes while old content remained. The pathname key also failed to distinguish project URLs. Shared Layout now owns one opacity transition (300ms exit / 650ms entrance). Its route props retain outgoing header geometry/theme/active navigation until exit; outgoing content is inert. Legacy wrappers receive neutral variants, preserving Work source/body behavior. Query/hash changes do not remount pages. Initial presence is disabled so server-rendered content is visible without JavaScript.
- Lenis, analytics and booking embed remain mounted outside the keyed Layout. Removed the unused legacy `visited` cookie state/timer from `_app`; it was disconnected from HomeIntro. Home's restored per-tab session gate remains authoritative.
- Home/Our Way image load handlers run a 700ms opacity fade as pixels arrive, including late images; existing transform/blur and intro/height animations remain. Reduced-motion checks skip route/image fades; unavailable Web Animations leaves images visible. Reduced-motion and failed-image paths source-reviewed, not newly OS-emulated/throttled.
- In-app desktop Our Way → Contact sampled outgoing opacity 1 → .796 → .446 → .072 while header stayed light/Our Way; incoming Contact mounted at opacity 0 with dark/Contact header, rose through .162/.520/.821/.962 to 1. Contact → Our Way image opacity 0 → .189 → .394 → .606 → .825 → .991 alongside the page fade. Page opacity reached 1; hero settles to viewport minus 32px as before. Timing samples include variable local route-loading latency, not a performance benchmark.
- Mobile 402×874: menu opens with Close focused, navigates to Work, closes and restores body positioning/overflow. Work Artsy returns the same four project URLs with route wrapper `/work`, opacity 1 and no query-triggered route remount. Home return does not replay the intro. Our Way final screenshot visually reviewed; no horizontal overflow. Default viewport restored afterward.
- Scrolled navigation initially exposed Next's early scroll reset. Shared Navigation/Header/Footer logo links and mobile router pushes now defer that reset for different routes. Mobile Home → Our Way sample retained scrollY=1748 throughout outgoing opacity 1/.687/.315/.006; new Our Way mounted at opacity 0 and scrollY=0. Same-page links retain their ordinary scroll behavior. Legacy body links were not edited; their existing Next scroll behavior is outside this shared-navigation adjustment.
- Contact booking opens the existing modal with Close focused; Escape returns focus to the CTA and restores root inert/body overflow. Existing embedded-calendar loading blocker remains. No booking submitted.
- Read-only settled-route assertions PASS (one present route, opacity1, matching Home header state, no inert/intro/scroll lock, no horizontal overflow); reusable `validation/page-transitions-check.js` added. Initial production HTML for Home/Our Way/Contact/Work verifies shared wrapper opacity1.
- Final isolated `npm run build:redesign`: PASS (`/tmp/otherness-transitions-final-build.log`). Targeted isolated ESLint with browser environment and both Hooks rules: 0 errors, one inherited `_app` explicit-any warning. Explicit `tsc --noEmit --incremental false`: same 11 inherited errors, none in changed files (`/tmp/otherness-transitions-types.log`). `npm run verify:redesign`: PASS, all 181 expected hashes/data assertions. `git diff --check`: PASS. Studio unchanged; inherited Studio/global-lint/project-hydration failures remain. Full phase-8 media/accessibility matrix not claimed.
- Local only on `codex/site-redesign`; no CMS/asset/dependency/deployment/staging changes or phase advancement. Remaining dependencies: Contact embedded-calendar and final editorial/artwork acceptance. Next prompt: “Review page transitions at localhost:3010 and apply the next requested adjustment; keep Home intro once per tab.”

# Start here — Otherness redesign

## Session rule

**One phase per conversation.** Read SPEC.md, ASSETS.md and VALIDATION.md, implement the requested phase only, update this handoff, then stop. Do not automatically create tasks or begin the following phase.

## Current state

- **Home/Contact staging promotion authorized, 24 September 2026:** Tayte requested pushing the four reviewed changes to staging: visible first-load intro pairs, Results image motion, booking fallback-button removal and Contact footer social hover transitions. Fresh origin/staging matches `7752171`; normal fast-forward promotion is available. Final isolated build/hosted verification in progress. Preserve the independent Services summary max-width edit and supplied font originals locally; master remains `2707efa`.

- **Home intro / Results / Contact follow-up, 24 September 2026:** fixed invisible first-load words by isolating their AnimatePresence from the route’s `initial={false}`; all three pairs visibly animate on desktop/mobile, retaining once-per-tab playback. Results background images now sharpen from 4px blur over 1s and scale 1.05→1 over the shared 5s slide duration, pausing offscreen and disabling motion for reduced motion. Removed the injected “Open booking page” modal button at Tayte’s request. Contact-only footer socials invert to black/white over 300ms on hover/keyboard focus. Browser checks, targeted lint and preservation checks pass; intro/Results/modal production build passes, followed by lint/browser checks for the CSS-only footer change. Same 11 inherited frontend type errors. Local only on `codex/site-redesign`; no staging/CMS/phase change. Contact calendar still loads indefinitely in the in-app browser; device/editorial/artwork acceptance remains. Next prompt: “Review the Home intro, Results motion and Contact footer at localhost:3010; apply only the next requested QA adjustment.”

- **Mobile QA staging deployment verified, 23 September 2026:** `7752171a95799f7c96e44d13508266e6aec5d69c` is on `origin/staging`; Vercel `4JmyXZ9eKdGEYu6gfsxTVhBwexgK` completed successfully. https://otherness-staging.vercel.app serves the mobile-only fixes. Hosted 402px checks confirm transparent Menu/blur6px, transformed logo and consultation gap8px; 1512px confirms original desktop SVG glass/8% tint/no logo transform. Home/Our Way/Contact/Work HTTP200 with noindex. Master remains `2707efa`. Device scroll acceptance and existing Contact/editorial dependencies remain; next: retest on iPhone.

- **Mobile QA staging promotion authorized, 23 September 2026:** Tayte requested pushing the completed mobile-only fixes to staging, overriding branch isolation for this promotion. Final isolated production build, targeted lint and all 181 preservation hashes pass. Fresh remote staging equals `df526da`, allowing a normal fast-forward; master remains `2707efa`. Deploy transparent/6px mobile Menu blur, transform-based mobile Home logo and mobile consultation gap8px, with original desktop glass/scroll styles preserved. Verify hosted deployment before completion; physical iPhone, Contact calendar and editorial/artwork acceptance remain.

- **Mobile-only scope correction, 23 September 2026:** Tayte clarified that this entire QA request was for iOS phones. Restored desktop header’s original SVG liquid glass, 8% idle tint, interaction tints and original logo scroll treatment. Transparent fill/6px blur, transform-based Home logo animation and 8px consultation arrow gap now apply only at the existing mobile breakpoint (≤768px). This supersedes desktop changes described in the entries below. In-app desktop/mobile computed styles and mobile geometry, targeted lint, 181 preservation hashes and diff checks pass. Local only; no deployment. Remaining device/Contact/editorial acceptance unchanged. Next: continue mobile-only iPhone QA without changing desktop.

- **Menu tint refinement, 23 September 2026:** made only the mobile Menu button fully transparent in idle/hover/pressed states, retaining 6px blur per Tayte’s clarification; desktop navigation retains 65% fill and 12px blur. In-app computed styles, targeted lint and diff checks pass. Local only; prior iPhone retest and acceptance dependencies remain. Next: continue requested iPhone QA.

- **iOS header/CTA follow-up, 23 September 2026:** removed SVG liquid-glass refraction; shared header controls use 65% theme-aware fill and standard/WebKit 12px backdrop blur (75% hover, 85% press). Home wordmark keeps expanded layout dimensions and uses translate/scale for scroll instead of changing width/top/margin, preserving landing size, final half-crop and navigation gaps. Our Way consultation arrow now has an explicit 8px gap. In-app mobile/desktop geometry, menu, reverse-scroll and six-width CTA checks pass; isolated build, targeted lint and 181 preservation hashes pass; same 11 inherited frontend type errors. Physical iPhone smoothness remains to be retested; responsive in-app checks are not an iOS performance measurement. Local only on `codex/site-redesign`, no staging/CMS/phase changes. Contact calendar and editorial/artwork acceptance remain open. Next prompt: “Retest the header scroll and consultation spacing on iPhone; apply only the next requested QA adjustment.”

- **Transition/intro staging deployment verified, 23 September 2026:** application commit `df526dac93729aac02998d8aa8fb3a3f0cb7562b` is on `origin/staging`; Vercel deployment `7wcVoDoFxyWgJfpfiz4UTPkK7zP4` completed successfully at https://otherness-staging.vercel.app. Home/Our Way/Contact/Work HTTP200, noindex and new visible route wrapper pass. Hosted in-app first-visit intro, refresh skip and Home → Our Way page/hero fades pass. Master remains `2707efa`; existing Cal/editorial/artwork acceptance issues remain. This verified status supersedes local-only/deployment-pending notes below. Next: review the changes on staging; continue only requested QA.

- **Transition/intro staging deployment authorized, 23 September 2026:** Tayte requested deploying the completed transition fixes and once-per-tab intro. Fresh fetch shows `origin/staging` equals local base `ff343ff`, with no divergence. Promote the reviewed changes using a normal fast-forward push; master remains `2707efa`. Final isolated build and browser/type/preservation validation are recorded below. Verify the resulting hosted deployment before calling this complete.

- **Page transition follow-up, 23 September 2026:** shared Layout now fades header/page/footer together (300ms out, 650ms in), retaining outgoing route/theme until hidden. Covers Our Way/Contact/Home and legacy routes without changing Work bodies; pathname keys distinguish project URLs but preserve shallow filters. Shared links defer scroll reset until exit completes. Home/Our Way hero images fade on load for 700ms, respecting reduced motion. Lenis, analytics and booking embed stay mounted. Removed unused legacy `visited` cookie code; once-per-tab intro remains. In-app desktop/mobile route/theme/hero/scroll/menu/filter and modal-dismissal checks, final isolated build, targeted lint and preservation checks pass; same 11 inherited frontend type errors. Local only, no deployment/phase advancement. Contact embedded-calendar loading and editorial/artwork acceptance remain open. Next prompt: “Review page transitions at localhost:3010 and apply the next requested adjustment; keep Home intro once per tab.”

- **Home intro testing ended, 23 September 2026:** Tayte confirmed once-per-tab playback. Restored the original `otherness:intro:v1` session-storage admission gate, including unavailable-storage/restored-scroll bypasses; current 7.2s choreography and Escape/reduced-motion behavior retained. This supersedes all earlier temporary intro-replay dependencies below. In-app first visit, refresh, Home → Our Way → Home and separate-tab admission checks pass; targeted ESLint/Hooks and diff checks pass. Local only on `codex/site-redesign`, no deployment or phase advancement. Inherited type/lint/hydration issues, Contact embedded-calendar acceptance and editorial/artwork dependencies remain. Next prompt: “Continue requested Otherness QA at localhost:3010; keep Home intro once per browser-tab session.”

- **Staging promotion authorized, 23 September 2026:** Tayte explicitly requested pushing the current site to `staging`, superseding the earlier no-staging restriction for this promotion. Include all current Home/Our Way/shared-shell/Contact refinements, Work CTA removal and earlier Home fade timings. `origin/staging` is an ancestor of the redesign branch, so use a normal fast-forward push, never force. Keep `master` unchanged. Existing Contact embedded-calendar acceptance and editorial/artwork/intro-replay dependencies remain; this is staging review, not production launch.

- **Hero timing follow-up, 23 September 2026:** Home sentence/header now start at 5.8s/6.5s (500ms earlier), retaining 700ms fades; intro completion follows at 7.2s. Image entrance unchanged. Our Way already shrinks after the 1.5s image reveal over 800ms to `100vh - 32px`; verified the 32px next-section reveal at mobile and desktop sizes, no Our Way change needed. Targeted lint/diff checks pass. Local only; inherited issues, Contact/editorial/artwork acceptance and temporary Home intro replay remain. Next prompt: “Continue hero animation QA at localhost:3010 and apply the next requested adjustment.” No phase advancement.

- **Work listing CTA removal, 23 September 2026:** explicit requested exception to Work-body preservation. Removed “Transform your complexities into strengths.” and joined the two project grids. All 16 cards/order retained; former split now uses 64px desktop / 32px mobile row gaps. In-app six-width continuity/overflow checks, mobile visual/keyboard checks, Artsy filter, production build and targeted component lint pass; same 11 inherited type errors. Verification now pins the two approved changed files while preserving the original baseline and checking the other 179 legacy files. Local only; no CMS/deploy/phase change. Existing Contact booking acceptance, artwork/editorial and Home intro replay dependencies remain. Next prompt: “Continue Work QA at localhost:3010/work and apply the next requested adjustment.”

- **Phase 7 Contact / Cal.com, 23 September 2026:** implemented `/contact` from Tayte’s desktop design with responsive adaptation and compact white footer. Contact nav now points here. Hardcoded `otherness/discovery` using the supplied HTML embed; shared footer CTAs, Home service links and Our Way consultation use it. No new CMS fields or packages. Six-width overflow checks, modal opening/dismissal/focus/scroll, build, targeted lint and 181 preservation hashes pass; same 11 inherited frontend type errors. **Booking acceptance is still open:** the in-app browser leaves the external iframe blank/loading, including an isolated plain-iframe test. Direct Cal page loads the 30-minute discovery event and available slots; an accessible direct-link fallback is present and native-click navigation verified. No appointment submitted. Local only; no push/deploy/staging change. See latest VALIDATION.md. Next: resolve/verify the Cal iframe loading in phase 7, then request Contact visual acceptance; do not start phase 8 yet.

- **Our Way mobile header/scrollbar QA, 23 September 2026:** replaced the shared interior mobile header’s fixed -10px crop with half the rendered wordmark height, matching settled Home. Process scrollbars are hidden; native scrolling, snapping and keyboard access remain. In-app 375/402/768px checks, 1512px desktop crop and Home/Our Work comparison pass. Targeted lint, diff checks and all 181 preservation hashes pass. Local only; inherited issues and Contact/editorial/artwork/Home intro acceptance dependencies remain. Next prompt: “Continue Our Way mobile QA at localhost:3010 and apply the next requested adjustment.” No phase advancement.

- **Our Way scroll/header QA, 23 September 2026:** process track now bypasses Lenis for touch only, preventing vertical wheel input from switching to native scrolling mid-animation. All 58 process/credentials list entries reuse Body Large (Neue Montreal 18/27px, normal/400, 0.18px tracking). Shared header active links use solid white/dark inverse fills without underlines; text/fill transitions are 300ms. Our Way explicitly marks every section's theme and samples wordmark/menu positions independently, including the covering introduction over the parallax hero. Desktop/mobile section sweep, six-width typography/overflow checks, horizontal track and menu checks, production build, targeted lint and 181 preservation hashes pass; same 11 inherited frontend type errors. Local only. Next prompt: “Continue Our Way QA at localhost:3010 and apply the next requested adjustment.” Contact/editorial/artwork and Home intro acceptance dependencies remain; no phase advancement.

- **Our Way process content, 23 September 2026:** published “Our process together”, four stage titles and 17 service items from Tayte’s `process-tgoetehr.jpg`, without links. Existing introduction, stable stage keys and every other Our Way field preserved; all 656 other document revisions unchanged. CMS-only follow-up; no deployment or phase advancement. Next: continue requested Our Way QA; existing Contact/editorial/intro acceptance dependencies remain.

- **Our Way hero QA, 23 September 2026:** removed Scroll down; 1.5s image blur/scale entrance followed by 800ms height recession to `100vh - 32px`, then Home-like scroll parallax/zoom/blur. Sentence fades in after 1s (700ms); scroll fades/blurs it before header overlap and restores it on reverse scroll. Reduced motion skips animations; restored scroll skips entrance. Following sections cover the slower hero. Desktop/mobile in-app sequence/geometry checks, isolated production build, targeted lint and 181 preservation hashes pass; same 11 inherited frontend type errors. Local only. Next: continue requested Our Way QA; existing editorial/Contact/intro acceptance dependencies remain.

- **Our Way credentials QA, 23 September 2026:** published 16 services, 14 clients and 11 recognition entries from Tayte’s `Home About.jpg`, in order and without links (including repeated CreativeBoom). Existing five logos and every other Our Way field preserved; all 656 other document revisions unchanged. Removed Pause/Resume button and its state/styles; hover/focus pause and reduced-motion layout remain. In-app rendered counts/assets, targeted lint, 181 preservation hashes and diff checks pass. No deploy or phase advancement. Next: continue requested Our Way QA; Contact designs and existing acceptance dependencies remain.

- **Expanded header on all pages, 23 September 2026:** removed non-home 100px compact trigger and desktop/mobile compact styles from the active shared `redesign/Header.tsx`. Wordmark/navigation stay expanded on scroll. Home choreography and footer fade remain. In-app Our Way desktop/mobile scroll checks and targeted lint/diff checks pass. Next: continue requested QA; existing dependencies remain.

- **Hero entrance head start, 23 September 2026:** image blur/scale starts at 4.6s instead of 4.8s, 200ms before the loader finishes fading. Image duration remains 1.5s; phrase/header and completion timing are unchanged. Targeted lint/diff checks pass; local in-app reload completes and restores heading focus. Local only. Next: continue requested Home QA; existing intro acceptance/Contact/artwork dependencies remain.

- **Mobile bottom footer reveal restored, 23 September 2026:** removed the mobile rule hiding the white wordmark/navigation strip and its spacer. All widths now reuse the desktop fixed-bottom reveal, measured spacer and covered-link inert protection. Mobile legal gaps remain 16px. In-app 375/402/768px geometry/overflow and forward/reverse reveal checks pass; targeted lint/diff checks pass. Local only. Next: continue requested Home QA.

- **Mobile Noticed/footer QA, 23 September 2026:** first Noticed accordion opens by default, with thumbnail beside source/year and a separate destination arrow. Reuses Services height + blur/fade entry/exit, with inert closed panels and quick-reversal height release. Mobile footer spacing now 64px top, 96px icon→heading and booking→socials, 64px socials→legal, 16px legal gaps, 24px bottom. Desktop presentation retained. Responsive/keyboard checks, targeted lint, isolated build and 181 preservation hashes pass; same 11 inherited type errors. Local only. Next: continue requested mobile Home QA; existing Contact/artwork/intro acceptance dependencies remain.

- **Results layout stability, 23 September 2026:** mobile now reserves the tallest slide’s natural grid height, keeping logos top-aligned and quote bottoms 40px above the tabs. Switching quotes no longer moves the section, tab rail or following content. Logo dimensions reserve image space before loading. Identical before/after geometry verified at 375/402/768px; targeted lint passes. Local only; next: continue mobile Home QA.

- **Mobile Results QA, 23 September 2026:** removed fixed 760px stage height and hidden-slide height reservation on mobile. Quote→client rule is exactly 40px; active client stays centred at the original 24px inset, with next clients cycling to the right. Rail overflow is visible; clipping occurs at the viewport-wide Results section only. Click/keyboard and left/right swipes loop; manual selection pauses autoplay. Desktop layout preserved. Next: continue Tayte’s Home mobile QA; no phase advancement or CMS/deployment changes. Validation details below in VALIDATION.md.

- **Mobile menu glass removal, 23 September 2026:** removed liquid glass/backdrop filters from the three mobile menu links at Tayte’s request; restored plain 80% light translucent bars. Opening/200ms exit animations, black Close button and scroll behavior retained. Header glass unchanged. Local only; next: continue mobile Home QA.

- **Mobile menu exit/glass follow-up, 23 September 2026:** links now reuse `headerGlass` (8% tint, SVG refraction, blur fallback). Close/Escape and ordinary link selection run a 200ms fade/slide toward the trigger before modal release/navigation. Reduced motion closes immediately; cleanup cancels pending animations. In-app 402px checks, targeted lint and source preservation pass; same 11 inherited type errors. Local dev preview remains running. Next: continue Tayte’s mobile Home QA; no phase advancement.

- **Mobile menu QA, 23 September 2026:** transparent overlay with black Close bar and three centred translucent links matches the supplied `Menu Open.jpg` reference. Home scroll 0 opens upward; intermediate first-viewport scroll completes to at least 100vh over 450ms before opening downward. Scrolled Home/interior pages open downward in place. Home mobile wordmark-to-button gap is 12px. Native focus trapping, Escape, route/desktop-resize close and scroll restoration retained. In-app mobile checks, isolated production build, targeted lint and all 181 preservation hashes pass; same 11 inherited type errors. No CMS/deployment/phase change. Dev server remains on localhost:3010. Next: continue Tayte’s Home mobile QA; Contact designs and final artwork/intro replay acceptance remain pending.

- **Footer refinement, 22 September 2026:** social links now use 8px/16px padding and Neue Montreal 12px/120%, 500, 0.96px tracking. Right-side copy matches Tayte’s supplied design with 40px gaps. Desktop white wordmark/navigation is fixed beneath the scrolling dark footer with measured reserved space; shared header fades as the dark footer approaches and returns on reverse scroll. Existing mobile omission of the white strip retained. Build, targeted lint, 181 preservation hashes and six-width in-app checks pass; same 11 inherited type errors. Dev server freshly started at localhost:3010. Copy is a local presentation override; CMS documents untouched. No phase advancement. Next: review footer locally or use the existing phase 7 Contact dependency prompt.

- **Results carousel follow-up, 22 September 2026:** Results autoplays with a 5s linear underline timeline; next slide fade/blurs in (1s). Clicking or keyboard-selecting a tab stops autoplay. Slides are stacked so copy/tab layout does not shift. Logo is 120px × auto; 320px between the Results heading and logo/quote. Reduced motion disables autoplay and blur/fade. Local only; next work remains requested QA or the phase 7 Contact dependency prompt.

- **Results fields follow-up, 22 September 2026:** Home New results no longer have an attribution field. Remaining fields are client name, quote, client logo, background and optional mobile background. Tabs render client name; the logo area shows only the logo (or a labelled placeholder). Schema, GROQ projection, types, seed and content checks updated. Local HTML at localhost:3010 shows Medable / Lorem Ipsum tabs and no attribution copy. Hosted Studio still shows the old field until this schema is deployed. Local only; next work remains requested QA or the phase 7 Contact dependency prompt.

- **Services card typography follow-up, 22 September 2026:** card titles now reuse `.heading-small`; subtitles use new shared redesign `.type-h5` (Baryton 20/26px, normal/400). Image/title/subtitle gaps are 10px and tallest-content-to-rule clearance is 64px at all six checked widths, including mobile (previously 48px). Targeted lint, source preservation and browser computed styles pass. Local only; next work remains requested QA or the phase 7 Contact dependency prompt.

- **Services QA, 22 September 2026:** accordion headers now use a twelve-column grid; shared redesign `.type-h3` is Baryton 40/54px and `.type-large` is Neue Montreal 18/27px. Literal Baryton `-`/`+` controls are 48/64.8px. Native horizontal snap carousels extend to the viewport edge with 24px gaps and four-column desktop cards. Active/inactive rules use paper/taupe tokens. Services and Home introduction share `.button-primary` (12px padding, 10px type); paragraph-to-button gap is 32px. Six responsive widths, keyboard behavior, build, isolated lint and 181 preservation hashes pass; same 11 inherited type errors. Published service art/copy and introduction copy have arrived; content issues are now 38. Local only at localhost:3010; further requested QA or the existing phase 7 dependency prompt remains next.

- **Hero entrance sequencing, 22 September 2026:** loader → image scales 1.04→1 and clears 6px blur over **1.5s** → centred phrase blur/fades in immediately for 0.7s → header blur/fades in for 0.7s. Original Baryton typography preserved. Escape cancels all entrance effects and restores focus/scroll; reduced-motion bypass remains. Local-only follow-up, no phase advancement. See latest VALIDATION.md entry; intro replay remains enabled until acceptance.

- **Introduction spacing/type QA, 22 September 2026:** desktop spacing now 196/64/64/128px, reusable scoped `.heading-small` matches the supplied 14px/700/148%/0.56px uppercase style, and What to expect uses 10px/700/120%/0.4px type with 12px padding and an 8px gap. Mobile retains 96px/64px outer spacing. Published agency statement is still empty; labelled placeholder retained. Six-width in-app checks, CTA navigation, targeted lint, production build and all 181 preservation hashes pass; same 11 inherited type errors. Local only at localhost:3010; further requested QA or the phase 7 dependency prompt below remains next.

- **Hero motion tuning, 22 September 2026:** increased image zoom to 16% and blur to 12px. The centred title now smoothly fades/blurs away based on actual logo/title clearance, completing before overlap; scrolling up restores it. Desktop, mobile and short-screen checks, targeted lint and isolated production build pass; same 11 inherited type errors. Intro replay, parallax, locked wordmark, colour crossfade and independently updated glass navigation are preserved. Local preview only; no next phase started.

- **Header glass visual correction, 22 September 2026:** matched the supplied screenshot with square, borderless, shadowless controls, 8% white tint, no black fill and no hover/press translation. Refraction retained and strengthened (SVG displacement 18); blur reduced to 0.4px (2px standard-filter fallback). Hover fades only background colour with a fixed filter. Local preview only; latest validation below supersedes the original glass appearance.

- **Header glass follow-up, 22 September 2026:** live shared header navigation and mobile Menu use CSS/SVG liquid glass inspired by https://liquid-glass.ybouane.com/. Implemented in `components/redesign/HeaderGlass.tsx` and the active redesign Header (the linked legacy layout/Header is unused). Local only at localhost:3010. Build/lint/source preservation pass; same 11 inherited frontend type errors. See VALIDATION.md. Existing hero QA edits remain intact; phase 7/8 not started.

- **Latest local follow-up: Home hero QA, 22 September 2026.** Home now has subtle image zoom/blur/darkening and slower vertical parallax with following sections above it. The half-cropped home wordmark/navigation stays fixed beyond 100vh on desktop/mobile, with a 300ms crossfade between light/dark wordmarks over Home sections. Intro replay is temporarily enabled on every refresh/return for Tayte's testing; restore the session gate after acceptance. Build, targeted lint, all 181 preservation hashes and six-width browser checks pass; the same 11 inherited frontend type errors remain and published landing artwork reduces content issues to 72. These changes are local, not deployed; use `http://localhost:3010`. See the latest VALIDATION.md entry. This follow-up does not start phase 7 or 8.
- Branch: `codex/site-redesign`, based on `origin/staging` commit `2707efad642d6be8a6c5461351851b10e7bd96f3`.
- Phases 1–6 implementation is complete. Phase 7 Contact is implemented locally; embedded booking acceptance remains pending. Phase 8 is not started. Final artwork/copy acceptance remains pending.
- This branch is now pushed to the same-named remote branch for its dedicated preview. No upstream is configured. Always use an explicit redesign branch push; never push to staging/master.
- Three additive **published** singletons are seeded in `vdwu088q/production`: `homePageV2`, `ourWayPage`, `siteSettingsV2`. Existing drafts/publications are preserved by idempotent seed tooling. All 642 pre-existing non-draft CMS documents retain their original revisions.
- Additive Studio schemas, Website Redesign group, fixed-ID editors and singleton action/creation restrictions are deployed at https://otherness.sanity.studio/.
- Seven new schema types, separate nullable frontend contracts, fixed-ID queries and a fresh published settings loader are in place. See DATA.md for usage and seed provenance.
- Work bodies, fonts, global styles, legacy schemas/queries/types and checked-in settings JSON are preserved. Verification covers 181 baseline hashes, normalizing only additive shell props in the two Work pages; shared Layout is intentionally replaced.
- Shared header/footer/menu, scoped Neue Montreal web fonts, favicon/manifest and supplied OG default are integrated. Home introduction/services/results/Noticed now render published Home New content. Landing/session intro and the Our Way page are implemented.

## Local commands

```sh
cd frontend
npm ci --legacy-peer-deps --no-audit --no-fund
# If .env.local is missing, copy .env.redesign.example to .env.local.
npm run dev:redesign
npm run build:redesign
npm run check:redesign
npm run check:redesign:release
npm run verify:redesign
npm run verify:redesign:chrome # after build
```

Local preview is `http://localhost:3010`. Public read-only Sanity config is in `.env.redesign.example`; `.env.local` is ignored. No token is required to read published content. NEVER place a Sanity write token in NEXT_PUBLIC variables.

Both redesign commands bypass legacy `buildJson`. Build diagnostics are written to ignored `frontend/.redesign/content.json`; future page `getStaticProps` should use `getRedesignData` / `getRedesignSettings` from `frontend/lib/redesign/server.ts`. Do not import the stale legacy JSON into new chrome.

`check:redesign:release` intentionally fails with 76 missing copy/artwork/alt fields. Preview builds remain usable. Empty content is nullable; components must use labelled development placeholders where needed and omit optional imagery. No images were copied from legacy content or reference screenshots.

Studio:

```sh
cd studio
yarn install --frozen-lockfile
SANITY_STUDIO_TELEMETRY_DISABLED=1 yarn dev --port 3333
node scripts/verify-redesign.cjs
```

Port 3333 is an existing allowed CORS origin. The authenticated browser UI could not be inspected because the in-app Sanity session was logged out; CLI schema compilation/build/deploy and authenticated data checks passed. Do not claim UI editing was tested. The alternate Studio npm lockfile is still stale.

## Preview hosting and rebuild

- Existing Vercel project `otherness`, team `tayteco-36dd2d0b`, root `frontend`, production branch `master`, Node 24.
- Existing Production → `master` and Staging → `staging` hooks remain unchanged. No Sanity automatic webhooks existed; none were added.
- Added **Website Redesign** Vercel deploy hook for **`codex/site-redesign` only** and a same-named manual deploy entry in Studio's Vercel tool. It reuses the existing Studio Vercel credential in the private `vercel-deploy.*` namespace. No credential or hook URL is committed.
- Branch-local `frontend/vercel.json` selects `npm run build:redesign` and deterministic npm installation. The wrapper forces noindex and uses the deployment's own `VERCEL_BRANCH_URL` / `VERCEL_URL` for canonical/sitemap origin, avoiding staging's inherited SITE_URL.
- Stable preview: https://otherness-git-codex-site-redesign-tayteco-36dd2d0b.vercel.app
- First hosted builds exposed missing legacy peer imports. `@react-spring/three@9.7.3` and `react-is@18.2.0` are now explicit dependencies, using versions already in both lockfiles; no framework or package version upgrades.
- Hosted final validation is recorded in VALIDATION.md.
- Verified staging alias remains on `dpl_EzsX8AUMtSntpbBQNUrH9Q9WDg6N`; public `www.otherness.design` remains on `dpl_2hW8KVM2WdqKzcff48GcNwxsi41B`.

## Phase 3 preview

Implementation commit `6c7605c` is deployed READY as `dpl_DNhdLzJjwpgkmZTt8g6nf7ouiqZE` at the existing stable preview. Hosted chrome/menu/metadata/assets/noindex checks pass. Staging and production retain their original deployment IDs. Documentation/capture follow-up commits contain no application changes.

## Phase 3 implementation notes

- Every concrete static page loads fresh published settings through `getRedesignShellProps()` in `frontend/lib/redesign/shell.ts`. No App.getInitialProps or client settings fetch was added. Keep this additive shell prop on new phase 4/6 pages.
- Shared chrome lives in `frontend/components/redesign/`; Layout keeps the legacy Main and Lenis behavior. Work's historical header footprint remains stable to preserve body positions. Neue Montreal is scoped; never change Work's global font variables.
- Native dialog handles the mobile modal/inert background. Custom focus cycle, Escape, route/resize close, focus return and body/Lenis scroll restoration are verified. Call `lenis.resize()` after unlocking body before restoring its scroll state.
- Current primary nav is Our Work / Our Way / Contact. `/our-way` intentionally remains a **404 until phase 6**. Contact uses fresh published `consultationUrl`. `/working-together` remains 200 with no redirect.
- Legacy homepage duplicate header/menu were removed; a single shared header is present, white over its existing hero. Final small/bottom landing wordmark and scroll choreography are phase 5.
- Footer socials are Instagram, LinkedIn and Email from published settings. Trademark is omitted while null. Copyright is the current CMS value (“Otherness”), not invented legal text.
- One absolute default OG image uses the supplied JPEG and preview build origin. Project overrides remain; article metadata access was corrected to the actual asset shape. All 18 populated overrides pass rendered-HTML verification.
- Original font files remain untouched/untracked. Three converted WOFF2 assets (400/500/700) are committed. See ASSETS.md and `validation/phase3-fonts.json`.
- Both localhost:3010 and the dedicated stable preview were missing from Sanity CORS, breaking legacy filters. Added these two exact origins with credentials disabled; the seven previous origins remain. No content/schema or deploy-hook changes.

## Validation and dependencies

Frontend/Studio builds, targeted lint (including Hooks), data/source preservation and rendered metadata checks pass. Frontend typecheck has **11 inherited errors** (two article metadata errors fixed), Studio still has 5. The invalid global ESLint config and existing project hydration bugs remain. Release content check still reports 76 missing fields.

Responsive checks passed at 375/402/768/1024/1512/1920. Menu keyboard/focus/scroll/route/resize behavior, Work mood/type/reset filtering, archive redirect, Privacy and actual Contact/social destinations were checked in the in-app browser. Phase 1 Medable/Artem desktop baselines were found to be blank transition captures; use source preservation and the new captures, not a claim of exact image matching. Full media/subproject edge cases remain phase 8. See VALIDATION.md for precise limits and outputs.

Approved new copy, trademark, principles, recognition, clean artwork and Contact designs remain pending. Neue Montreal core web weights are ready. Seeded service selections still need review.

## Phase 4 preview

Final application commit `9a5d4ef` is deployed READY as `dpl_7t8MJmVxcFmNPPDCn5HE2z92FakM` at the stable preview. Hosted in-app checks verify the four sections, accordion/Noticed behavior, preview-origin OG metadata and final thumbnail clearance. HTTP 200/noindex confirmed. Staging and public production retain their original deployment IDs. Follow-up documentation/capture changes contain no application changes.

## Phase 4 implementation notes

- `frontend/components/redesign/HomeSections.tsx` and its scoped styles render introduction, services/project tracks, results tabs and Noticed. Homepage loads `homePageV2` through `getRedesignData()` and retains `getRedesignShellProps()` for chrome.
- Branding starts open; one service can open or all can close. CMS ordering is identical on desktop/mobile. Every referenced project remains in the native mobile scroll track; unresolved/archived references render without a link.
- Results switch background, client logo, quote and attribution as one keyed panel. Arrow keys wrap; Home/End select edges; click/tap works; no autoplay. Only one Medable testimonial is currently published. Two-result and null-document behavior was verified with a temporary local fixture (saved as `.tsx.txt` under validation, removed from routes before build).
- Noticed uses all ten real published entries. Desktop hover/focus reveals artwork; mobile starts with the first entry open and keeps expansion separate from navigation. Services/Noticed controls are at least 44px.
- `Artwork.tsx` requests responsive Sanity image widths, applies editorial crop and positions the hotspot. Absent assets/alt or failed images render labelled development placeholders. No legacy artwork was imported into the four new sections; the untouched legacy HomeHero remains only until phase 5.
- Introduction statement, service descriptions/artwork/captions, results artwork/logo and Noticed thumbnails remain editorial dependencies. Optional captions are omitted. No CMS documents, schema, hooks, shared chrome, Work code, font source files or global styles were changed.
- Phase 4 validation passes: builds, targeted lint, metadata, nullable fixtures, six widths, accordion/Noticed keyboard and click behavior, horizontal card scrolling, results keyboard/click switching, real Aero project navigation. Same 11 frontend / 5 Studio type errors and 76 release content issues remain. See VALIDATION.md for limits.
- Legacy HomeHero still includes its old booking URL and a Mux hydration warning; phase 5 replaces that entire hero. No loader/session logic or Our Way route was introduced.

## Phase 5 implementation notes

- `HomeLanding.tsx` replaces legacy HomeHero on `/`; the homepage no longer fetches the legacy Home document or renders its Mux player/old booking link. Published loading pairs/landing statement use the existing Home New contract. Phase 4 sections and all Work bodies/fonts/interactions are unchanged.
- Landing is one static responsive picture with crop/hotspot handling and eager loading. Clean artwork is still absent: an explicit development placeholder is rendered. No screenshot crops or legacy photography were used. Baryton statement, icon and smaller wordmark match the reference positioning.
- `HomeIntro.tsx` uses Framer Motion for three 1.5s blur/fade pairs and a final 0.3s fade. Session storage is marked at admission, so refresh and return navigation do not replay it. Missing pairs, unavailable storage, restored deep scroll and reduced motion bypass it. A timer ends it independently of media/animation callbacks; effect cleanup restores scrolling/focus and cancels timers.
- **Tayte requested removal of the visible Skip intro button during this phase.** It is removed; Escape and reduced-motion bypass remain. SPEC.md records this change. The intro uses a native modal dialog focused on the panel while playing. Main content is server-rendered with initial opacity 1, independently of the intro.
- One shared header/navigation moves from the landing’s bottom position to the oversized top-cropped wordmark, then compact sticky navigation. Home-only CSS/scroll handling leaves the existing interior/Work footprint intact. Scroll listeners are passive and use a single animation frame; resize and motion preference changes are handled and cleaned up.
- Validation: builds, isolated lint, preservation and metadata checks pass. Same 11 frontend / 5 Studio inherited type errors and 76 release-content issues. Six responsive widths, timing, Escape, refresh, return, reduced-motion/storage/null/failed-image fixtures and menu restoration checked using only the in-app browser. See VALIDATION.md for exact boundaries.
- No CMS/schema/hooks/dependencies/font originals/global-style changes. Our Way and Contact were not implemented.

## Phase 5 preview

Application commit `3b62e34` is READY as `dpl_FuWWrzzYDbqeiKktQyCZFfVYLV5N` at the stable redesign preview. Hosted in-app verification confirms the intro has no visible skip control, automatically completes, does not replay on refresh, and renders the mobile landing without overflow. HTTP 200/noindex and one absolute preview-origin OG image pass. Staging and production retain their original deployment IDs. Documentation/capture follow-up commits contain no application changes. Browser viewport overrides were reset; preview remains open. Local server/keep-awake session were stopped. Supplied font originals and the inherited untracked phase 4 capture remain untouched.

## Phase 6 implementation notes

- `/our-way` now renders all seven page sections, then the shared footer. It uses fresh published `ourWayPage` data through `getRedesignData()` and additive shared shell props. No schema, query, Studio or CMS content changes were required.
- `OurWay.tsx` and `OurWay.styles.ts` contain the scoped page. Hero supports eager responsive imagery, separate mobile art, crop/hotspot and failure fallback. The shared header adds white wordmark/nav treatment only while over the Our Way hero; Home phases 4–5 and Work bodies/fonts remain unchanged.
- Partnership has ruled principles on the right and founder note at bottom left; mobile puts principles before the founder note. Four explicitly labelled principle placeholders preserve the layout while editorial content is absent. No screenshot copy was silently approved or published.
- Published four-stage process becomes a native horizontal snap track on mobile, with the next stage visible, keyboard scrolling and Lenis prevention for native track input. Consultation is desktop split/mobile stacked and uses the current published booking URL. Credentials retain the **entire** seeded founder biography, so this section is longer than the screenshot's single-paragraph draft. All biography, services, clients and recognition remain editable in the existing singleton.
- Recognition supports a gentle CSS loop, hover/focus pause, persistent Pause/Resume control, and static wrapping under reduced motion. Decorative duplicate logos have no links or focus stops and are hidden from accessibility. Published logos are currently absent; behavior was checked with clearly labelled local brand-file fixtures, removed before building.
- Validation passes: frontend/Studio builds, targeted lint, 181 legacy-source hashes, fixed-ID/null query/schema checks, 31 route shell checks and 18 existing image overrides. Same 11 frontend / 5 Studio inherited type errors; release gate still lists 76 content issues. Six responsive widths, scroll anchor/focus, mobile track, menu restoration, nullable content, failed hero image and recognition controls were checked in the in-app browser. See VALIDATION.md for precise limits.
- `/working-together` remains 200 without redirect. Contact remains the published consultation URL; phase 7 was not started. Supplied font originals and inherited phase 4 screenshot remain untouched/untracked. No dependency/global-style/font/hook changes.

## Phase 6 preview

Application commit `e204f364ff487fbf3943cba45b6f487962348c98` is READY as `dpl_28UpPgEPpKn48qGc5AUjjvz39rfk` at the existing stable preview. `/our-way` is 200/noindex with one absolute preview-origin OG image. Hosted desktop/mobile checks confirm all sections, four process stages, active Our Way nav, white hero header, menu/Escape and real Our Way → Work → Our Way navigation. `/working-together` remains 200 without redirect; removed fixture is 404. Vercel confirms staging remains `dpl_EzsX8AUMtSntpbBQNUrH9Q9WDg6N` and public www remains `dpl_2hW8KVM2WdqKzcff48GcNwxsi41B`. No aliases/hooks were changed. Documentation/capture follow-up contains no application changes. Viewport overrides reset; hosted preview left open. Local server and this task's keep-awake session stopped. Commits use only a command-scoped signing override.

## Next conversation: finish phase 7 validation

> Continue Otherness redesign Phase 7 on codex/site-redesign. Read docs/redesign/HANDOFF.md, SPEC.md, ASSETS.md and VALIDATION.md. Contact is implemented at localhost:3010/contact with hardcoded Cal.com otherness/discovery. Verify and resolve the embedded calendar loading: the in-app browser currently shows only a loader/about:blank iframe although the direct Cal event displays slots. Keep the accessible direct-booking fallback. Review Contact desktop/mobile appearance with Tayte. Preserve Work bodies, existing uncommitted QA changes, legacy CMS documents and staging. Update validation/handoff; do not start phase 8 or deploy without the requested scope.

After that acceptance, the next numbered phase is Phase 8 QA/launch, only when Tayte requests it.

## Future phase boundaries

3 shared chrome/branding → 4 Home sections → 5 landing/intro → 6 Our Way → 7 Contact when designed → 8 final QA/launch. See SPEC.md for approved details. Redirect `/working-together` only at launch.

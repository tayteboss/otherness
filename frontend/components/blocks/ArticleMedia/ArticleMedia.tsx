import styled from 'styled-components';

const ArticleMediaWrapper = styled.div``;

const ArticleMedia = () => {
	return <ArticleMediaWrapper>ArticleMedia</ArticleMediaWrapper>;
};

export default ArticleMedia;

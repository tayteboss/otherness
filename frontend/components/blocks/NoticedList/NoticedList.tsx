import styled from 'styled-components';

const NoticedListWrapper = styled.div``;

const NoticedList = () => {
	return <NoticedListWrapper>NoticedList</NoticedListWrapper>;
};

export default NoticedList;

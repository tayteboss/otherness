# Public read-only Sanity configuration. Never put write tokens in NEXT_PUBLIC_*.
NEXT_PUBLIC_SANITY_PROJECT_ID=vdwu088q
NEXT_PUBLIC_SANITY_DATASET=production
SITE_URL=http://localhost:3010
NEXT_PUBLIC_ENVIRONMENT=development
REDESIGN_PREVIEW=1

# For a hosted redesign preview, SITE_URL must be that preview's own URL.
# Launch is a later phase: review indexing and redirects before removing preview mode.
